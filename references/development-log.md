# Development Log

This file preserves chronological development evidence, failed-run diagnoses, verification notes, and status boundaries for anlin-writing.

Audience: developer/controller. Do not load this file for ordinary article generation or clean-eval first drafts.

Preservation rule: do not delete log entries. When reorganizing docs, move or rephrase only after preserving the factual content in git. Runtime docs and README may summarize this file, but this file remains the detailed audit trail.

## Latest Boundary Summary

- Target status: not yet proven to meet the `<=10%` stable impostor-identification target.
- Missing evidence package: 15 clean-eval generation cases plus calibrated `8 impostor + 2 placebo` blind rounds with low stable-identification and low placebo false-accusation rates.
- Latest repaired mechanism: compact `--repair-brief` fixed the specific terminal-only finalized-repair artifact failure by making the repair agent write back `finalized/draft.md`.
- Latest unresolved mechanism: the newest targeted retest still failed both checkpoints. Bounded generation stopped at preflight before checker call 1/2, and a finalized repair attempt left `draft.md` unchanged after trying to rediscover the skill reference from the finalized workspace.
- Next direction: retest the new inline finalized fallback in `SKILL.md`, then continue improving first-draft source guidance before expanding blind rounds.
- Current active-protocol recognition rate: `N/A - no valid 8 impostor + 2 placebo blind package yet`. The legacy `3 impostor + 1 placebo` diagnostic result remains historical evidence only and must not be reported as current target progress.
- Progress reporting rule: after every ten development iterations, report bounded status, finalized status, `blind_round_readiness`, valid blind package count, stable generated-sample identification rate or `N/A`, placebo false-accusation rate or `N/A`, invalid rounds, and the next blocking mechanism.
- Latest architecture change: clean-eval first drafts now route through the short `clean-eval-first-draft-minimum.md` before optional `standard-diary-source-engine.md`; the long `clean-generation-brief.md` remains available after wrapper findings as the detailed repair contract. This is meant to reduce pre-draft checklist overload while preserving all repair guidance.

## Preserved README Status Log

The skill is not yet proven to meet the `<=10%` stable impostor-identification target. The latest documented clean-eval cases still show bounded-generation, validation-scope, or tool-flow failures before blind rounds. The newest serious case family, `iteration-20260705-32` through `iteration-20260706-44` for `eval-07-2024-mothers-day-sincere`, reached some cleaner checker boundaries after the sincere-mode wrapper fix, but several runs still stop at preflight, fail to persist `draft.md` in the case workspace, or remain only reviewable under a small sincere stratum. The bundled style-profile is not applicable as a finalized short-genre pass/fail gate when the matching sincere stratum has too few originals and falls back to global priors. Blind-round preparation supports `--match-genre auto|standard|sincere|micro-hope|surreal` so short sincere/micro-hope drafts and their placebo rounds are compared against genre/length/line-count-nearer originals instead of random standard diaries. This fixes a validation confound; it is not a pass claim. No current report yet contains 15 clean-eval generation runs plus calibrated `8 impostor + 2 placebo` style rounds sufficient to claim the target.

Documentation architecture cleanup on 2026-07-07: README was kept GitHub/user-facing and model-agnostic; concrete model pools and failed-run detail remain in this development log. Active isolated judge templates moved to `references/judge-prompt-templates.md`. Runtime docs were audited to keep ordinary generation portable: no local machine paths, no provider/model-specific branches, no dependence on a private anti-slop skill, and no requirement that users possess the full 38-article corpus. This is documentation and architecture hygiene only, not new evidence that the `<=10%` target has been reached.

Strict blind-package correction on 2026-07-07: active runtime and validation docs now use only the calibrated `8 impostor + 2 placebo` blind-round package for readiness claims. The smaller smoke-tier language was removed from README, SKILL command examples, validation protocol, and controller judge templates. The old subagent-prompts compatibility pointer was deleted because the active file is `references/judge-prompt-templates.md`. Historical failed-run notes remain preserved in this log, including earlier smaller diagnostic rounds, but they are not current pass evidence.

Side-conversation handoff for main session on 2026-07-07: after this correction, the main session should treat `8 impostor + 2 placebo` as the only active blind-round readiness package. Do not resurrect the smaller smoke tier, do not recreate the old subagent-prompts compatibility filename, and do not add model-specific runtime branches. Continue from the same unresolved technical boundary: compact repair briefs fixed terminal-only artifact failure, but finalized repair still showed rhythm/texture review drift, so the next useful work is source-guidance and finalized-minimum repair simplification before running large blind rounds. Verification for this documentation/architecture correction must include active-doc scans for old small-tier commands and legacy prompt filenames, unit tests, script syntax checks, 38-original strict hard-gate calibration, and git status.

The current architecture pass is addressing source-level guidance, not only detection. The main risk found in review was that several deep repair references still used old "must / at least / until clear" wording that could make a generation agent treat review signals as content quotas. This has been tightened so clean-eval generation keeps a bounded generator role, while ordinary user mode can continue repairing hard errors and controller validation remains separate. Recent repair risk is no longer only rhythm ping-pong: a draft can pass the strict hard gate but remain style-profile `review` because repair overfills body, money, route, screen, food/weather, and explicit cognition. The runtime now treats this as a thinning/source-repair problem, not a reason to add more features. A compact `references/standard-diary-source-engine.md` is now part of the minimal clean-eval pack for standard diary only, so a fresh generator sees a short consequence-kernel engine immediately before the first draft instead of turning the longer clean brief into a visible plan/checklist.

Development tests should now rotate across multiple model surfaces and report the exact model string plus reasoning/thinking setting for each case. Current local controller pool: `deepseek/deepseek-v4-pro`, `deepseek/deepseek-v4-flash`, `mimo/mimo-v2.5`, `mimo/mimo-v2.5-pro`, `opencode-go/minimax-m3`, `gateway/gpt-5.5`, and `longcat/LongCat-2.0`. This is a controller protocol, not runtime prose advice: failures from different models are generalized into source/checker improvements, and no model-specific branches should be added to the generation instructions. When recent runs are imbalanced, choose the next clean-eval model from the lowest-use available surfaces before returning to high-use surfaces; the latest local usage audit found `deepseek-v4-flash` overrepresented and `longcat/LongCat-2.0`, `gateway/gpt-5.5`, `mimo/mimo-v2.5`, and `mimo/mimo-v2.5-pro` under-covered.

Local metadata audit for `iteration-20260707-82` through `iteration-20260707-90` found 10 recorded runs: `opencode-go/minimax-m3` 3, `deepseek/deepseek-v4-pro` 2, `deepseek/deepseek-v4-flash` 2, `longcat/LongCat-2.0` 1, `mimo/mimo-v2.5-pro` 1, `gateway/gpt-5.5` 1, and `mimo/mimo-v2.5` 0. For the next clean-eval generation, prefer an available zero/low-use model in this order: `mimo/mimo-v2.5`, `gateway/gpt-5.5`, `longcat/LongCat-2.0`, then `mimo/mimo-v2.5-pro`. Do not add model-specific generation branches; use model differences only to expose general source-shape, rhythm, fact-handling, or tool-flow failures.

Current source hypothesis after `iteration-20260707-90`: standard-diary failures are no longer mainly missing individual details. They start too often from the prompt object or from a private object ledger, then try to repair rhythm with comma-drag. `references/standard-diary-source-engine.md` now adds a compact "First Two Clusters" source order: practical snag first, prompt topic late, outside pressure still active, one small failure lands, then medium changes. This is a first-draft source order, not a five-item content quota, and should be tested before adding new detectors.

Follow-up attempt `iteration-20260707-91/eval-09-2024-classmate-wedding-invitation` selected `mimo/mimo-v2.5`, but the provider returned insufficient account balance before generation. It is recorded as unavailable, not as quality evidence. Follow-up retest `iteration-20260707-92/eval-09-2024-classmate-wedding-invitation` used `gateway/gpt-5.5`, `--variant high`, and thinking enabled after commit `fce845f`. The bounded run persisted `draft.md` and stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`), so it is not blind-round-ready. A trace-audit bug initially misclassified the formatted OpenCode `% Patch 1 file` write as "no draft written"; `check_clean_eval_trace.py` now accepts that formatted write while still flagging post-stop patches. Reanalysis reduced trace errors to 0 and hard errors from 2 to 1. The remaining source failure is clearer: period-row grid, too many short rows, and low natural connector spread. The hallway-aunt/sleeve scene was a valid public low-status consequence rather than missing roughness, so the checker now counts narrow `阿姨/邻居/楼道` dirty-sleeve/leak/smell action chains. Layer 0 also now teaches hallway witnesses as continuous action chains and adds "Cluster Grammar, Not Metrics" so standard-diary drafts generate connector turns from unfinished hand/reply/payment/door/body movement rather than punctuation repair.

Follow-up hardening after `iteration-20260707-94/eval-09-2024-classmate-wedding-invitation`: the bounded run with `longcat/LongCat-2.0` still stopped at preflight before formal checker call 1/2 with hard errors for thin connector coverage, weak rough/self-damage movement, social-decline indoor texture overfill, and missing breath points. The copied finalized repair cleared the old hard gate but remained style-profile `review` with red `punctuation`; manual reading showed the concrete failure was a punctuation pendulum, not mere punctuation count: the repair changed comma/line issues into a 70-line period-row grid (`没管。`, `删了。`, `没回。`, `算了。`) while room/object texture still carried too much of the wedding refusal. Current fix: Layer 0 now names `Punctuation Pendulum Guard`, social-decline prompts require the refusal aftermath itself as one load-bearing kernel, the hard checker reports `标准日寄句号网格` in generated draft-gate mode, and style-profile `review` with red `punctuation` now returns `punctuation_source_reset` instead of vague manual review. Verification for this node: 319 unit tests passed, 38 original corpus files had 0 ordinary strict hard errors, and style-profile calibration over 38 originals had `error_files=[]`. This is source/repair architecture progress, not a blind-round pass claim.

Follow-up retest `iteration-20260707-95/eval-09-2024-classmate-wedding-invitation` used `gateway/gpt-5.5`, `--variant high`, and thinking enabled after commit `8181e9b`. The bounded clean-eval run improved over iteration 94 because it reached the actual two-call checker boundary (`calls=2`, `preflights=2`) with zero trace errors after a trace-parser false positive was fixed. It still failed and is not blind-round-ready: the bounded final had one strict hard error, `社交拒绝纹理替代后果不足`, and style-profile remained `review` with red `line_rhythm` and `punctuation`. A copied finalized repair first attempted `mimo/mimo-v2.5-pro`, but the provider returned insufficient account balance; `longcat/LongCat-2.0` then produced a repaired draft but the OpenCode process hung and had to be stopped. Independent controller validation of the frozen finalized draft showed strict hard gate clean, but style-profile still `review` with red `line_rhythm` and `punctuation`, so diagnosis remains `systemic_gap`. Root fix from this case: Layer 0 now distinguishes a refusal chain from a refusal mention. Social-decline prompts must make a post-refusal response, wrong reply, old debt, payment/route hesitation, or body/social interruption change a later action; room objects such as paper, water, sleeve, bowl, charger, or sink can open the article but cannot replace the social engine. Finalized guidance now also states that a hard-gate-clean 40-50-line smooth prose article with too few earned short drops or rough long movement rows is still unresolved profile drift, not a clean final success.

Follow-up retest `iteration-20260707-96/eval-09-2024-classmate-wedding-invitation` used `longcat/LongCat-2.0`, `--variant high`, and thinking enabled after commit `012c096`. The bounded run regressed at the source layer: it stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`) with an overfilled 2227-character article, red style-profile families for `connectors`, `length`, `line_rhythm`, `ngram_texture`, and `punctuation`, plus hard findings including `标准日寄完整文章过满`, `标准日寄长行过密`, `标准日寄句号网格`, `具体纹理堆叠过密`, `社交拒绝编剧化回礼`, and `散文块压缩`. Manual audit found the new root failure: the previous "refusal chain" source fix was interpreted as permission to build a continuous plot. The draft expanded into multiple days,班群 photos, commute/地铁, coworkers,伴郎/喜酒 negotiation, and office-adjacent identity from the excuse `忙项目`. A copied finalized repair reduced the text to 940 characters but still failed with `标准日寄句号网格` and style-profile `review` with red `line_rhythm` and `punctuation`. Current fix: Layer 0 now gives the refusal chain an upper bound: one consequence transfer, one or two visible actions, same evening or one tiny next-day residue, then exit on a loose practical tail. The checker now reports `社交拒绝后果过度生长` for generated draft-gate social-decline samples that extend refusal aftermath into multi-day/commute/coworker/wedding-logistics plot. This remains source/repair hardening, not blind-round readiness.

Follow-up retest `iteration-20260707-97/eval-09-2024-classmate-wedding-invitation` used `gateway/gpt-5.5`, `--variant high`, and thinking enabled after commit `c8091df`; finalized repair used `opencode-go/minimax-m3`. The social-decline overgrowth fix worked in the bounded article: no伴郎/coworker/地铁/office-biography/multi-day wedding subplot remained, and length returned to 1092 body Chinese characters. The bounded checkpoint still stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`) with one hard issue, `标准日寄句号网格`; style-profile was `review` with red `punctuation`. The finalized repair also failed: it fixed the period-row grid by merging too many local rows into comma chains, producing hard `逗号密度过高` and style-profile `review` with red `punctuation`. Diagnosis remains `systemic_gap`, but the failure family has narrowed from social-decline plot overgrowth to punctuation-pendulum repair. Current fix: source and repair guidance now require local cluster surgery for period/comma drift: choose 3-5 unfinished hand/reply/payment/door/body/object spots, delete explanatory tails, rebuild only those into breathing clusters, and do not globally merge rows into comma-heavy paragraphs or split the whole article into hard-stop captions.

Follow-up retest `iteration-20260707-98/eval-09-2024-classmate-wedding-invitation` used `longcat/LongCat-2.0`, `--variant high`, and thinking enabled after commit `133224d`; finalized repair first attempted `deepseek/deepseek-v4-pro` but the provider returned `Insufficient Balance`, then used `opencode-go/minimax-m3`. The bounded run still stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`) and remained not blind-round-ready. It did not relapse into multi-day wedding plot, but LongCat again wrote the first submission as a 22-line prose block, then used rhythm tooling and rewriting to reach 49 lines while preserving a period-row surface. Hard gate reported `高频词覆盖不足`, `段落发动机信号偏弱`, `标准日寄句号网格`, and the trace found `节奏脚本后重写未重跑节奏修复`; style-profile stayed `revise` with red `connectors`, `line_rhythm`, `ngram_texture`, and `punctuation`. The finalized repair did not converge either: Minimax read the full 31-warning style-profile output, drafted a proposed replacement in the terminal/log, then kept reasoning about individual repeats and never wrote the repaired article back to `draft.md`, so the draft remained unchanged and failed the same gates. Current fix: `check_style_profile.py` now has `--repair-brief`, a compact generator-facing profile interface that folds long metric output into source actions and explicitly requires writing a complete revised `draft.md` before further metric analysis. Under `--strict --repair-brief`, a nonzero exit is an ordinary not-pass repair signal, not a tool failure; the repair agent should write back `draft.md` before doing any further metric analysis. Finalized repair instructions now use the brief, while controller validation can still rerun the full profile report after the artifact is frozen.

Targeted artifact retest `iteration-20260707-99/eval-09-2024-classmate-wedding-invitation-repairbrief` copied the iteration-98 bounded draft into a new `finalized/` workspace and used `opencode-go/minimax-m3` with the compact repair brief. The artifact failure improved: `finalized/draft.md` changed from the bounded input, the repair log showed no checker source/test/hidden-threshold reads, and independent hard gate passed with only info findings. It is still not a quality pass: independent style-profile validation remained `review` / `checkpoint_pass=false` / `formal_gate:not_pass`, with red `line_rhythm` and `punctuation` plus yellow `cognitive_mechanism`, `ngram_texture`, `structure`, and `texture`. Controller summary stayed `systemic_gap` / `not_ready_for_blind_rounds`. This means the brief fixed the terminal-only artifact failure mechanism, but finalized repair still over-reasons and bounces in rhythm/texture; next work should simplify the finalized minimum repair path and then return to first-draft source guidance, not expand blind rounds yet.

Follow-up retest `iteration-20260708-100/eval-09-2024-classmate-wedding-invitation` used `longcat/LongCat-2.0`, `--variant high`, and thinking enabled for bounded generation, then `deepseek/deepseek-v4-flash` for finalized repair. Bounded generation remained invalid before blind rounds: it stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`) with hard findings for an AI binary reframe, thin connector coverage, weak paragraph engine, period-row grid, and social-decline consequence overgrowth. The first submission was a 17-line prose block with `不是...是...` reframe and a screen/refusal chronology; the bounded final reached 49 lines but still relied on room/water texture while the refusal aftermath did not become load-bearing. Finalized repair improved the artifact protocol because `finalized/draft.md` changed and hard gate had zero errors, but it still failed the formal profile gate: style-profile stayed `revise` with red `connectors`, `line_rhythm`, `ngram_texture`, and `punctuation`, plus yellow `cognitive_mechanism`, `structure`, and `texture`. Controller diagnosis remains `systemic_gap` / `not_ready_for_blind_rounds`. Current fix from this case: `check_style_profile.py --repair-brief` now includes an artifact contract and one `primary_source_rewrite` so a repairing agent does not treat five metric families as five TODOs; finalized repair minimum now forbids self-discovery, TODO-style analysis, and full-profile metric reasoning before writeback; Layer 0 first-draft guidance now forbids prose-block escape hatches and adds a water-drip test for social-decline drafts whose side object replaces the refusal engine. This is source/repair architecture progress, not a pass claim.

Follow-up retest `iteration-20260708-102/eval-09-2024-classmate-wedding-invitation` first attempted `mimo/mimo-v2.5`, but the provider returned insufficient account balance and produced no `draft.md`, so it is recorded as unavailable rather than quality evidence. The next bounded run used `gateway/gpt-5.5`, `--variant high`, and thinking enabled after commit `19a87e5`. It remained invalid before blind rounds: `calls=0`, `preflights=3`, trace had no hard errors but one visible-process warning, strict hard gate had three errors (`粗粝自毁信号不足`, `段落发动机信号偏弱`, `逗号密度过高`), and style-profile improved from the previous `revise` boundary to `review` with red `connectors` and `punctuation` only. Manual read: the article was no longer a prose-block first submission and the social refusal chain was more visible, but it still overused comma-drag and did not make the low-status/social engine strong enough to reach checker call 1/2. A copied finalized repair using `deepseek/deepseek-v4-pro` failed as an artifact: `finalized/draft.md` hash stayed unchanged, and the log showed the agent tried to glob/search the parent skill directory for `references/finalized-repair-minimum.md`; permission was denied and no repair was written. Controller summary classified the full case as `systemic_gap` / `not_ready_for_blind_rounds`. Current fix from this case: the finalized minimum fallback is now embedded directly in `SKILL.md`. If `references/finalized-repair-minimum.md` is not directly available through the already-triggered skill, the agent must not search parent or sibling directories; it should read current `draft.md`, use formal gates only if `<skill-dir>` is already known, write a complete revised `draft.md`, and mark local validation unresolved rather than exiting with unchanged artifact.

Follow-up retest `iteration-20260707-90/eval-03-2023-valentines-alone` used `longcat/LongCat-2.0`, `--variant high`, and thinking enabled for the bounded clean-eval checkpoint after commit `8c30bf8`. The run is still not blind-round-ready. Bounded clean-eval stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`) with strict hard errors for `高频词覆盖不足` and `逗号密度过高`; style-profile stayed `revise` with red `connectors`, `line_rhythm`, `ngram_texture`, and `punctuation`. The copied finalized repair used `gateway/gpt-5.5` and produced a gate-pass-shaped article, but the repair log read/grepped checker source and threshold constants, including `check_anlin_violations.py`, `check_style_profile.py`, and `YELLOW_REVIEW_FAMILY_THRESHOLD|SOFT_REVISE_FAMILY_THRESHOLD`. The updated controller now records this finalized checkpoint as `invalid` with 13 trace contamination errors, so the case diagnosis is `systemic_gap`, not a clean convergence sample. Current fix: finalized repair mode now forbids checker source, tests, or hidden threshold inspection, and `summarize_dev_checkpoints.py --finalized-trace-log` marks such runs invalid, including logs with null-byte OpenCode stream output. The source problem remains first-draft rhythm and connector movement: standard diary needs natural action/turn engines before preflight, not comma-drag repair.

Follow-up retest `iteration-20260707-89/eval-03-2023-valentines-alone` used `opencode-go/minimax-m3`, `--variant high`, and thinking enabled after commit `2d8fb30`. The bounded run reached the two actual clean-eval checker calls (`calls=2`, `preflights=1`) with zero trace hard errors, so the tool-flow boundary improved. It still is not blind-round-ready: style-profile remained `revise` with red `connectors`, `ngram_texture`, and `punctuation`, and manual review found period-row finish, repeated `已经` glue, proof-object ledger texture, and the learned administrative ending `今天先这样。`. Controller recheck showed the previous `段落发动机信号偏弱` hard finding was a checker undercount rather than a source absence: the article had rider/manager/rent public nodes that were not all counted. Current fix: public/social texture and paragraph-engine signals now include plain handoff, rider wait, building manager, landlord/rent, and small public consequence terms; `style-profile.json` is rebuilt as version `1.6`; `今天先这样。` / `就先这样。` are treated as learned ending buttons in source guidance, clean brief, feature budget, strict checker, and clean-run preflight. This is a public-hinge counting fix plus a source-tail hardening, not a target pass claim.

Follow-up retest `iteration-20260707-86/eval-03-2023-valentines-alone` used `deepseek/deepseek-v4-flash`, `--variant high`, and thinking enabled after commit `8a48c0c`. The short-line caption-grid failure improved: the bounded final had 58 body lines and about 962 body Chinese characters rather than an 80-100 row grid. The run still failed before formal checker call 1/2 (`calls=0`, `preflights=3`) with strict errors for `高频词覆盖不足`, `段落发动机信号偏弱`, and the clean-eval stop boundary; style-profile stayed `revise` with red `connectors`, `line_rhythm`, and `punctuation`. Manual/root review found a new protocol-source failure: after the first `draft.md` write, the generator repeatedly ran homemade `python -c` / `Measure-Object` metric probes for body characters, line counts, line lengths, connector lists, period ratios, and short drops, then overwrote `draft.md` many times before the first wrapper. A finalized repair attempt with `deepseek/deepseek-v4-pro` timed out while rediscovering the skill and did not modify `draft.md`, so it is recorded as tool execution timeout, not as repair-quality evidence. Current fix: the Clean-Eval Minimum and clean brief now state that after the first clean-eval `draft.md` write, the next metric tool must be `clean_run_checker.py`; no `python -c`, `Measure-Object`, `wc`, homemade regex counters, line-length distributions, connector counters, or repeated pre-wrapper overwrites are allowed. `check_clean_eval_trace.py` now reports `clean-eval自建指标预检` and `clean-eval首个wrapper前多次改写draft` as errors.

Follow-up retest `iteration-20260707-87/eval-03-2023-valentines-alone` used `deepseek/deepseek-v4-flash`, `--variant high`, and thinking enabled after commit `d2f98fa`. The previous self-metric probe failure did not recur, so the wrapper-interface fix worked. The bounded run still failed before formal checker call 1/2 (`calls=0`, `preflights=3`, stop reason `preflight`): the final bounded draft had about 1082 body Chinese characters and 75 content lines, but zero long action/speech/thought rows, too many sealed period rows, weak paragraph engine, missing rough self-damage, and style-profile `revise` with red `connectors`, `line_rhythm`, `ngram_texture`, and `punctuation`. Trace validation also caught a real tool-flow error: after a rhythm script, the generator rewrote `draft.md` and called the wrapper without rerunning the relevant rhythm repair. Manual audit found the deeper source problem: the draft did not contain obvious AI reframe phrases, but it let `灯管/充电线/外卖漏油/香菜/裤腿油印` become a private proof ledger. The rider contact pointed out the leak, then disappeared; later oil inspection, sink, phone, and lamp repetitions did not create a public consequence or rough social lowering. Current fix: the standard diary source engine now says a public hinge must leave a mark before it disappears; a later private inspection does not turn a contact into social consequence. The clean brief now states that any later `Write draft.md`, `Edit draft.md`, `Set-Content`, or `Out-File` cancels prior rhythm-script repair. This remains source-guidance/tool-flow work, not blind-round readiness.

Follow-up retest `iteration-20260707-88/eval-03-2023-valentines-alone` used `deepseek/deepseek-v4-pro`, `--variant high`, and thinking enabled after commit `abec6a7`. The source-engine fix improved the article substantially: the final bounded draft moved from private oil inspection into an active handoff where the rider asks for paper, the room is exposed, the door/muffler problem changes the next action, and the title `门缝` comes from a side consequence rather than a prompt prop. It still stopped before formal checker call 1/2 (`calls=0`, `preflights=3`) and is not blind-round-ready. The last preflight message was only `short_breath_lines=3 < 4`, but strict hard gate still showed `高频词覆盖不足`; style-profile stayed `revise` with red `connectors`, `line_rhythm`, and `ngram_texture`, and trace validation still caught the stale rhythm-script pattern from the first preflight cycle. Controller recheck with a fresh no-state copy showed that the bounded draft should enter formal `checker call 1/2` under the updated wrapper rather than being stopped only for a 3/4 short-breath near miss. Current fix: `clean_run_checker.py` now lets a single `short_breath_lines=3 < 4` near miss reach the formal checker while still blocking 0-2 short-breath grids, and its `NEXT_ACTION` text now explicitly says any later write/edit after `rebalance_line_rhythm.py` requires rerunning `rebalance_line_rhythm.py` before the wrapper. This is a clean-eval boundary/tool-flow correction plus source-progress evidence, not a target pass claim.

Follow-up retest `iteration-20260707-85/eval-03-2023-valentines-alone` used `deepseek/deepseek-v4-pro`, `--variant high`, and thinking enabled after commit `61b880e`. The checker-source contamination from `iteration-84` did not recur, so the trace-hardening fix worked. The bounded run still failed before formal checker call 1/2 (`calls=0`, `preflights=3`): the first saved draft had about 733 body Chinese characters, 95 body lines, and zero natural long action/speech/thought rows; the bounded final grew to about 908 characters but kept the same short-line caption-grid source shape. Trace validation also caught a stale rhythm repair: the generator ran a rhythm script, rewrote `draft.md`, then called the wrapper without rerunning the relevant rhythm script. Current fix: the clean-eval minimum and `standard-diary-source-engine.md` now say line count is not the target, a 700-900 character article split into 80-100 tiny rows is a generated caption grid, and standard-diary first drafts should be 6-8 breathing clusters with several rough long rows before the first file write. The clean brief and SKILL entry also make the rhythm-script ordering rule explicit: after any rewrite, the next wrapper call is valid only if the relevant rhythm script is rerun or no rewrite happened after the script. This remains source-guidance and tool-flow work, not blind-round readiness.

Follow-up retest `iteration-20260707-84/eval-03-2023-valentines-alone` used `opencode-go/minimax-m3`, `--variant high`, and thinking enabled after commit `638e9cc`; `mimo/mimo-v2.5` and `mimo/mimo-v2.5-pro` were unavailable because the provider returned insufficient balance. This run improved the previous bounded failure: it reached the actual two-call clean-eval boundary (`calls=2`, `preflights=2`) instead of stopping before checker call 1/2. It still is not blind-round-ready. The final bounded draft retained a hard `逗号密度过高` error and style-profile `revise` with red `punctuation`, `line_rhythm`, and `ngram_texture`; manual review also found repeated phone/water/sleeve/food packets and an over-accounted line rhythm. The trace exposed a protocol gap: after preflight, the generator grepped `early_comma_ratio` and read `clean_run_checker.py`, so this sample is contaminated as a clean-eval run even though the article artifact exists. Current fix: `check_clean_eval_trace.py` now parses `opencode export` session JSON, accepts absolute `draft.md` paths when they are the current external case workspace, and flags checker-source inspection; runtime guidance now says the wrapper message is the full repair interface and source repair must not reverse-engineer checker internals.

Follow-up retest `iteration-20260707-81/eval-03-2023-valentines-alone` used `deepseek/deepseek-v4-flash`, `--variant high`, and thinking enabled after commit `587ee52`. Trace validation had zero hard errors; the new compact standard-diary source engine prevented the earlier first-wrapper rhythm-script contamination and produced a persisted `draft.md`. Bounded still stopped at `CLEAN_RUN_PREFLIGHT_STOP` before formal checker call 1/2 (`calls=0`, `preflights=3`), and the copied finalized repair with `deepseek/deepseek-v4-pro` also failed (`systemic_gap`): strict hard gate still had `段落发动机信号偏弱`, and style-profile stayed `review` with red `punctuation` and `ngram_texture`. Manual audit found the shared source failure: after escaping the prompt-prop title loop, the drafts let one replacement material family (`油/油印/袖口/裤子/桌面/碗`) own title, middle, sink, clothing, and tail, while social exchange and crooked interpretation stayed too thin. Current fix: the standard diary source engine and clean brief now say one material family cannot own all kernels; a public hinge should open a second consequence rather than authorize more private inspection. Finalized repair guidance now forbids arguing that the checker regex is limited when hard gates fail; repair must replace the source engine or record unresolved failure. This is systemic source/repair work, not blind-round readiness.

Follow-up retest `iteration-20260707-79/eval-03-2023-valentines-alone` used the same private-holiday/takeout prompt and `deepseek/deepseek-v4-flash` after commit `2bca03a`. It still stopped at `CLEAN_RUN_PREFLIGHT_STOP` before formal checker call 1/2 (`calls=0`, `preflights=3`), so it is not blind-round-ready. The run improved on gaze-first contact but exposed a sharper source failure: the model treated private oil stains, mirror face, hair, burp, and clothing inspection as if they satisfied rough low-status movement, while the rider had already left and no transaction, door, reply, body movement, or social position changed. It also kept falling between short-line grid and period-row grid instead of building breathing clusters at the source. Current fix: standard-diary generation now has an early source contract before short-genre rules, private grime is explicitly not an event unless it touches a public hinge, and `clean_run_checker.py` reports `private_grime_without_public_consequence` with repair guidance that forbids adding another stain/mirror/burp/hair/smell packet. This is still pre-blind-round source guidance work, not a target-pass claim.

Follow-up retest `iteration-20260707-78/eval-03-2023-valentines-alone` used the same prompt and `deepseek/deepseek-v4-flash` after commit `86a85bc`. It improved length and removed the earlier feed-inventory opening and pure ambient tail, but still stopped at `CLEAN_RUN_PREFLIGHT_STOP` before formal checker call 1/2 (`calls=0`, `preflights=3`). The draft became a complete but over-smooth sentence-row article: high period density, too few true short drops, weak rough/social engine, no crooked interpretation/humor friction, and a repeated gaze-first rider contact (`往下看/瞥了一眼`) that still did not change the next action. Current fix: clean-eval source guidance now says not to write the gaze line first for riders/cashiers/neighbors/shopkeepers; start the contact from bag/payment/door/reply/hand/body consequence. `clean_run_checker.py` preflight now reports `period_row_grid` for standard drafts that are mostly one finished `。` sentence per row, so the generator gets a source-shape reset before formal checker calls. This remains pre-blind-round work, not a pass claim.

Latest source/preflight update after `iteration-20260707-77/eval-03-2023-valentines-alone`: the bounded `deepseek/deepseek-v4-flash` run still stopped at `CLEAN_RUN_PREFLIGHT_STOP` before formal checker call 1/2 (`calls=0`, `preflights=3`), so it remains a source-guidance failure and is not blind-round-ready. The draft improved title selection and avoided the one-line feed inventory, but it stayed underbuilt at 859 body Chinese characters, kept a soft delivery witness (`look/sweep -> private stain notice -> rider leaves`), used too few real short drops/long action rows, and ended on the ambient line `外面路灯还亮着。`. Current fix: Layer 0 now requires the first draft to choose private consequence kernels before writing, where each load-bearing cluster has a real consequence verb such as wait, ask, point, hold, fail, leak, close, drop, wipe, hide, pay, answer, leave, or change route/room/reply/body. Look/glance/sweep/notice/silence/light/sound are not engines by themselves. `clean_run_checker.py` preflight now reports `pure_ambient_ending` before a formal checker call, and trace validation no longer treats quoted reference text like "Write the complete titled article..." as a nonrelative write action. This is source and validation-integrity work, not a target-pass claim.

Latest source/repair update after `iteration-20260706-64/eval-03-2023-valentines-alone`: the bounded `mimo/mimo-v2.5-pro` run still stopped at preflight before checker call 1/2, and the copied finalized repair remained unresolved with comma-density / line-rhythm / punctuation drift. Manual audit found a systemic gap: a standard diary prompt with a generic mother message was routed toward the short-sincere corridor, and the repair path tried to create long rows through comma chains, producing `逗号密度过高`. Current fix: genre locks are now explicit for all known formal cases, including `--genre standard`; generic `朋友圈 + 妈妈发消息/吃了吗` is no longer enough to infer short sincere without direct Mother's Day/care-memory evidence; first-draft guidance distinguishes real long action/speech/thought rows from comma-drag; finalized repair guidance treats `逗号密度过高` as the opposite of comma repair. This is a source-routing and repair-path correction, not blind-round readiness.

Latest source update after `iteration-20260706-65/eval-03-2023-valentines-alone`: trace validation now no longer treats an absolute file-tool display title as a nonrelative write when the actual `filePath` is `draft.md`. The bounded case is therefore a real bounded failure rather than a path-invalid run: it reached the two-checker boundary with zero strict hard errors but style-profile `revise`; finalized repair with `deepseek/deepseek-v4-pro` cleared strict hard gate but remained style-profile `review` with five yellow drift families, so it is still not blind-round-ready. Manual audit found the source failure: a standard private-holiday/wrong-food prompt let the prompt prop become title, opening engine, and tail proof (`备注`/`香菜` loop). Current fix: Layer 0/1 now treats standard prompt-prop title loops as a source reset problem, and `check_anlin_violations.py` plus `clean_run_checker.py` report `标准日寄提示物标题闭环` / `standard_prompt_prop_title_loop` in generated-draft mode. Repair guidance now says to retitle from a side consequence, cut one prop echo, and rebuild through door/hallway/payment/dirty-hand/wrong-reply/sink/rider/shopkeeper/other-person reaction rather than only changing title or punctuation. This is a stronger source-guidance fix, not a target-pass claim.

Follow-up retest `iteration-20260706-66/eval-03-2023-valentines-alone` used `deepseek/deepseek-v4-flash`, `--variant high`, and thinking enabled after commit `0c7fe4c`. The run produced no `draft.md`: the model loaded the clean-generation brief, then spent the entire response enumerating candidate lines, calculating character totals, and revising the arithmetic until the step ended by length. Trace validation correctly reports missing `draft.md` and missing `clean_run_checker.py`; this is an execution/source-guidance failure, not a style-quality sample. Root fix: runtime guidance still keeps rough length/line corridors, but now explicitly forbids line-numbered drafts, character-count arithmetic, `Total:` / `I need about` metric work, and metric tables before saving. Exact counting belongs to `clean_run_checker.py` and the controller; generation should use a quick page-shape glance, persist the best complete `draft.md`, then let scripts audit it. This reduces checklist/metric overfitting without deleting the statistical audit layer.

Follow-up retest `iteration-20260706-67/eval-03-2023-valentines-alone` used the same model after the no-arithmetic fix. It improved from the previous no-draft failure to a persisted `draft.md`, but the bounded article failed as a prose-block rebound: final body had only 14 content lines, long-line and punctuation red drift, and style-profile `revise`. Trace review also found a checker false positive: the log used a combined PowerShell command that checked `.anlin-clean-eval-mode` and `Get-Location` before the first write, but `check_clean_eval_trace.py` did not recognize that current-directory confirmation. Current fixes: trace validation now accepts marker/location checks in one shell command; `clean_run_checker.py` records the frozen stop-boundary hash and no longer overwrites `bounded_final` if `draft.md` is mutated after stop; Layer 0 and `clean-generation-brief.md` now state that the file content being written must already be a line-broken article, not a 10-15 paragraph short story. This is source-shape and validation-integrity work, not a blind-round pass claim.

Follow-up retest `iteration-20260706-69/eval-03-2023-valentines-alone` used `deepseek/deepseek-v4-flash`, `--variant high`, and thinking enabled after commit `5cb8931`. The run wrote `draft.md` to the external case workspace and avoided the previous no-draft / prose-block rebound, but it still stopped at `CLEAN_RUN_PREFLIGHT_STOP` before formal checker call 1/2 (`calls=0`, `preflights=3`). The final bounded draft had about 910 body Chinese characters, 67 body lines, only 3 long lines, weak paragraph engine, missing rough self-damage, and style-profile `revise` with red line-rhythm, punctuation, n-gram texture, and a hard `被看见` slop finding caused by `备注大概根本就没被看见`. Controller review treats this as source guidance progress but not a quality pass. Current fix: `被看见` now ignores literal object/message/note contexts such as a note or order not being seen, while still flagging therapy-humanizer uses; Layer 0 and `clean-generation-brief.md` now make 600-850 character standard-diary candidates a source failure before first write and require outside witnesses to touch the ugly fact or change action, not merely look once while the narrator privately notices a stain. This is a stricter source-level repair; the case remains not blind-round-ready.

Follow-up retest `iteration-20260706-70/eval-03-2023-valentines-alone` used the same model after commit `07f69c1`. The final bounded draft improved to about 959 body Chinese characters and 57 body lines, but still stopped at `CLEAN_RUN_PREFLIGHT_STOP` with `calls=0`; the remaining preflight block was `paragraph_engine=weak`. Controller hard/style review showed the underlying issue more clearly: the draft still used prompt-prop title `备注`, three em-dash explanation hinges, thin connector coverage, weak paragraph engine, no real dialogue/social texture, high period density, and repeated n-gram templates. Current fix: clean preflight now reports exact standard prompt-prop titles and em-dash surfaces before formal checker calls, and Layer 0 warns against `——` explanation hinges. This is a source/preflight improvement, not blind-round readiness.

Latest source architecture update after `iteration-20260706-70`: the remaining failure is not solved by adding one more outside contact or one more dirty object. The draft still followed a quiet chronology (`phone/feed -> order food -> wrong item -> wash bowl -> bed`) where the delivery handoff and hallway details could be deleted without changing the next action. Layer 0 now requires standard-diary first drafts to grow from 2-3 load-bearing action clusters before the first `draft.md`: each cluster must change reply, body state, route, payment, room position, or social position, and at least one cluster should not be the prompt's obvious screen/order/object surface. This is source guidance, not a background quota; it must not make games, places, platforms, or corpus facts mandatory. `clean_run_checker.py` now points `paragraph_engine=weak` and underbuilt-source repairs toward rebuilding load-bearing clusters instead of patching isolated symptoms or decorative texture. This remains pre-blind-round work.

Follow-up retest `iteration-20260706-71/eval-03-2023-valentines-alone` used `deepseek/deepseek-v4-flash`, `--variant high`, and thinking enabled after commit `60acdbc`. The run stayed protocol-aware at entry but did not create `draft.md` or call `clean_run_checker.py`; trace validation reports `clean-eval未写入draft.md` and `clean-eval未调用clean_run_checker`. The generator spent the response hand-counting body lines and estimated characters, then placed a candidate under a visible `Final version` code fence without persisting it. This is an invalid no-draft execution/source-guidance failure, not an article-quality sample. Current fix: Layer 0 now treats numeric length/line targets as a visual corridor only. Once a complete candidate exists, the generator must write `draft.md` immediately and let `clean_run_checker.py` count; hand-counting, add/subtract estimates, code-fenced candidate articles, and visible final-version drafting before file write are explicitly blocked.

Follow-up retest `iteration-20260706-72/eval-03-2023-valentines-alone` used the same `deepseek/deepseek-v4-flash` surface after commit `4f7085d`. The no-draft failure improved: the run wrote `draft.md`, used `clean_run_checker.py`, and respected the preflight stop boundary. It still stopped before formal checker call 1/2 (`calls=0`, `preflights=3`). Manual/controller review found the source problem: the article opened by announcing the assignment surface (`情人节` / `朋友圈` / gift feed / `香菜` / `备注`) before any body, room, door, payment, sink, charger, or other practical action could create its own trouble; the outside witness line was also too soft because a glance did not change the next action. Current fix: Layer 0, `clean-generation-brief.md`, and `clean_run_checker.py` now treat high-signal private-holiday/social-feed openings as source resets. The first 8-12 body lines should start from a practical action that would still exist without the prompt, and `high_signal_opening=present` is caught in preflight before it consumes a formal checker call. This is a source-guidance fix, not blind-round readiness.

Follow-up retest `iteration-20260706-73/eval-03-2023-valentines-alone` used the same model after commit `26de165`. The high-signal opening fix worked in the narrow sense: the bounded draft no longer opened with `情人节/朋友圈/香菜/备注`, and trace validation had no hard errors. It still stopped before formal checker call 1/2 (`calls=0`, `preflights=3`), so it is not a blind-round candidate. Strict hard gate reported `高频词覆盖不足`, `标准日寄长行缓冲不足`, `粗粝自毁信号不足`, `段落发动机信号偏弱`, and the clean stop boundary; style-profile stayed `revise` with red connector, line-rhythm, n-gram, and punctuation families. A copied finalized repair cleared strict hard errors but still remained style-profile `revise`, showing a broader source/repair gap rather than a checker-only issue. Manual audit: the model replaced the prompt opening with a quiet chronology and a soft delivery witness (`looked once -> narrator privately noticed pants/sleeve -> rider left`), then overused body/food texture, short closed rows, and connector glue such as `已经/然后/还是`. Current fix: source guidance and preflight now treat witness-only handoffs as failed movement. A rider, cashier, neighbor, or stranger must change payment, reply, bag/object state, dirty hand/clothing exposure, door movement, or the next action; otherwise the witness should be deleted or rebuilt as a consequence. This is still pre-blind-round work.

Follow-up retest `iteration-20260707-74/eval-03-2023-valentines-alone` used the same prompt and `deepseek/deepseek-v4-flash` after commit `705032b`. The soft-witness fix worked: the rider handoff now changed the scene through leaking soup, a visible oil stain, the rider's gaze, and the narrator making the stain worse. Bounded clean-eval still stopped before formal checker call 1/2 (`calls=0`, `preflights=3`), with remaining `early_comma_ratio` and roughness/closed-row rhythm problems; controller hard gate still reported long-line, roughness, and line-final comma failures, and style-profile stayed `review` with red `line_rhythm` and `punctuation`. The copied finalized repair cleared the hard checker only by adding more explicit self-labels such as `丢人` / `自己都嫌`, then skipped style-profile because the repair agent did not know where the bundled profile lived. Controller rerun with `references/style-profile.json` showed finalized remained `review` with red `line_rhythm` and `punctuation`, so this is a systemic source/repair gap, not readiness. Current fix: `check_style_profile.py` now defaults to the bundled profile, finalized guidance requires that profile audit, clean-run preflight treats early closed sentence rows as a source reset, and the hard checker recognizes public oil/soup-on-pants exposure seen by a rider as real roughness instead of rewarding explicit shame labels.

Follow-up retest `iteration-20260707-75/eval-03-2023-valentines-alone` used the same prompt and `deepseek/deepseek-v4-flash` after the bundled-profile fix. The bounded run still stopped at preflight before checker call 1/2 (`calls=0`, `preflights=3`), with a new visible source failure: title `备注栏` and the spine `朋友圈 -> 外卖备注 -> 香菜 -> 油污/红点` showed that double-prompt scenes can still let the prompt packet own title, body, and tail. The copied finalized repair ran the bundled style-profile audit, but remained `review` / `checkpoint_pass=false`; the repair agent still tried to describe it as near-success. Current fix: prompt-prop title-loop risk now catches `备注栏`, runtime generation weakens one prompt surface before drafting when romance-feed and wrong-food/order pressure combine, side-consequence titles are preferred, and `check_style_profile.py` now prints `formal_gate: not_pass` and returns nonzero under `--strict` whenever `checkpoint_pass=false`. Non-standard genre fallback is now also formal not-pass, not a strict-mode success. This is a validation-integrity and source-guidance fix; the target remains unmet.

Follow-up retest `iteration-20260707-76/eval-03-2023-valentines-alone` used `deepseek/deepseek-v4-flash`, `--variant high`, and thinking enabled after commit `c8a60f6`. The bounded run improved on the title loop (`油` instead of `备注栏`) and avoided obvious binary AI reframe sentences, but it still stopped at `CLEAN_RUN_PREFLIGHT_STOP` before formal checker call 1/2 (`calls=0`, `preflights=3`). The bounded draft remained a closed small story: `朋友圈 -> 外卖/order -> wrong-food residue -> rider -> wash/bed`, with a single-line feed inventory (`满屏都是花和转账截图`) and a weak oil/stain witness that did not reliably become a low-status action engine. The copied finalized repair cleared the hard checker but remained style-profile `review` / `formal_gate:not_pass`, mainly from punctuation and texture/structure drift. Current fix: source guidance now treats one-line social-feed inventories as prompt inventory, not a safe crop; `clean_run_checker.py` preflight reports `feed_inventory_opening`, and `check_anlin_violations.py --draft-gate` promotes `社交动态库存式开头`. Soft witness handling now also catches `扫到油渍`-style camera moves and no longer treats a narrator saying thanks as a real consequence; rider/cashier/neighbor looks must change payment, reply, bag/object state, door movement, dirty hand/clothing exposure, or the next action. This is still source-level pre-blind-round work, not a pass claim.

Latest source update after `iteration-20260706-48`: the indirect-sincere route now works, but the next failure moved to source repair. The bounded `mimo/mimo-v2.5` run produced a short sincere draft that repeated the same phone/message/bowl/water/oil packet after preflight, raising line count while preserving the same small-story spine; the separate finalized repair still failed and even began reading checker source to reason about hidden internals. Layer 0/1 now explicitly forbids short sincere local-packet loops, and the checker/preflight reports `短体裁局部材料回环` / `short_genre_local_packet_loop` when a repair repeats the same phone-light/message/chore/non-reply material instead of changing action. Ordinary/finalized repair guidance now also forbids reading checker source or tests to reverse-engineer rules. This is a systemic-gap fix, not a pass claim.

Follow-up retest `iteration-20260706-49/eval-07-2024-mothers-day-sincere` used `deepseek/deepseek-v4-flash`, `--variant high`, and thinking enabled. The trace controller initially misclassified the run as invalid because the generator wrote `draft.md` through a PowerShell here-string ending in `Set-Content -Path "draft.md"` after an invalid file-tool attempt; `check_clean_eval_trace.py` now recognizes that relative PowerShell write while still rejecting absolute or skill-directory write paths. Re-summarizing the run shows the bounded draft reached the two-call checker boundary with zero strict hard errors, but only `review` status: style-profile is inconclusive for the tiny sincere stratum and global fallback flags red connector and punctuation drift. Manual source review found the usable failure signal: the draft became a short sincere sentence-row grid, with too many sealed `。` rows and time glue such as `后来/已经/当时`. Layer 0/1 now teaches short sincere breathing clusters before drafting, and Layer 4 reports `短体裁句号网格` / `short_genre_period_grid` in generated draft-gate mode. This improves source guidance and validation accuracy; it is not a blind-round pass claim.

Finalized repair audit for the same case exposed a separate architecture gap: repair can mutate a short sincere draft into standard-diary bulk, adding relation exposition, neighbor/function-person drama, transfer/account material, and unsupported major family status such as `她不在了`. The checker now supports `--genre <standard|sincere|micro-hope|surreal>`, `clean_run_checker.py` accepts the same genre lock, and `summarize_dev_checkpoints.py` applies the effective genre to both hard-gate and style-profile reports. Source guidance now treats selected genre as frozen during repair and reports `短体裁误扩成标准日寄` plus `无依据重大家庭变故` in generated draft-gate mode. This closes a validator/source-routing gap; it is still not a target-pass claim.

Follow-up retest `iteration-20260706-50/eval-08-2024-gout-flareup` used `deepseek/deepseek-v4-pro` for bounded clean-eval after commit `af83bc7`. The bounded run had a clean trace and reached the two-call checker boundary, but failed: first submission was a compressed private case report (684 body Chinese characters, 19 lines), bounded final still had `strict: 单主题词密度偏高: illness`, and style-profile stayed `revise` with red connector, n-gram, and punctuation drift. A copied finalized repair using `deepseek/deepseek-v4-flash` cleared strict hard errors but remained style-profile `review` with red `ngram_texture` and `punctuation` plus yellow cognitive/connector/structure/texture drift, so the controller diagnosis is `systemic_gap` and not blind-round-ready. Root fix from this case: illness/body prompts now treat symptom/search/refrigerator/room-smell/ambient-sound loops as a source failure, require an exposed route/payment/doorway/shop/social consequence before drafting, and the checker reports `疾病病例报告闭环` in generated draft-gate mode when a standard diary remains a private case report.

Follow-up retest `iteration-20260706-51/eval-08-2024-gout-flareup` used `opencode-go/minimax-m3` after the private-case-report fix. The bounded run reached the two-call boundary and the new `疾病病例报告闭环` gate pushed the draft toward an external consequence, but the final bounded article still failed strict hard gate on `对话接力过密`, `粗粝自毁信号不足`, and `段落发动机信号偏弱`. A copied finalized repair using `mimo/mimo-v2.5` removed the obvious hard roughness findings by adding a public payment/spill sequence, but style-profile remained `review` with red `ngram_texture` and `punctuation`; manual audit showed the repair still repeated symptom/search/body/room packets to prove the same illness. Current fix: the checker now recognizes public payment/spill/waiting sequences as valid low-status engine signals only when tied to external transaction/social context, and a new generated draft-gate rule `疾病身体证明过密` blocks the second-order failure where external consequence exists but the body-illness packet still dominates. Source guidance now says not to repair weak roughness with `妈的`, `操`, or `我服了`; the event must change action, payment, route, reply, body, or another person's reaction. Rechecking that finalized draft now fails on `strict: 疾病身体证明过密`, so it remains a systemic-gap case and is not blind-round-ready.

Follow-up retest `iteration-20260706-52/eval-09-2024-classmate-wedding` used `deepseek/deepseek-v4-flash`, `--variant high`, and visible thinking. The bounded run stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`), so it is source/preflight evidence rather than evidence about the two-checker repair boundary. The first submission was an 825-character, 91-line short-line grid; after expansion and rebalancing the saved draft reached 952 characters and 70 lines, but style-profile stayed `review` with red `line_rhythm` and `punctuation`. Manual and deterministic review exposed two higher-priority source failures: the generator turned the refusal line `最近忙项目` into a real current workplace chain (`项目月底交付`, `组长`, `请假`), and it invented concrete old-campus geography/restaurant memory (`翠屏山出来那家川菜馆`, `西门外面那家烧烤摊`) to thicken the past. The checker now flags `无依据工作后果链` for project-excuse real-work chains even when the line says `群里`, and `无依据具体地标` for invented landmark/restaurant/campus-gate memory in generated draft-gate mode. Source guidance now says refusal excuses remain reply surfaces unless the user supplies real work facts, and old classmate memory should stay generic (`学校门口`, `那家店`, `以前吃饭的地方`) unless a specific name is supported. A copied finalized repair using `mimo/mimo-v2.5-pro` cleared those two unsupported-fact errors but still failed after review: the article repeatedly used 暖气、冷、手指、泡面、水龙头、洗手 and kitchen-window details to carry the social refusal, leaving style-profile at `review`. The checker now reports `社交拒绝室内冷感过密` when wedding/invitation/money pressure is present but indoor cold/food/body packets dominate the opening, middle, and tail. Rechecking the bounded draft now reports 3 hard errors, and the finalized draft reports 1 hard error plus style-profile `review`; this is a `systemic_gap`, not blind-round-ready.

Follow-up retest `iteration-20260706-53/eval-09-2024-classmate-wedding` used `mimo/mimo-v2.5-pro` after the social-decline room-texture guard. The bounded run remained invalid before blind rounds: it stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`), the bounded draft still had 3 strict hard errors and style-profile `review` with red `line_rhythm` and `punctuation`, and the trace contained a direct `check_anlin_violations.py` call after the clean wrapper. `check_clean_eval_trace.py` now flags this as `clean-eval直接调用普通checker`, because bounded clean-eval must use only `clean_run_checker.py`; otherwise the generator can see diagnostics outside the two-call protocol. The source failure family also persisted: diagnostic topic title (`狗哥的婚礼`), AI-style binary reframe, and indoor cold/noodle/washing texture taking over the social refusal. This iteration is protocol-contaminated source/preflight evidence, not a clean quality sample and not blind-round-ready.

Latest source update after `iteration-20260706-53`: social-decline prompts now use a stricter source order. The generator should choose a side action that can stand without the prompt, let the invitation enter as one cropped message surface, and title the article from the damaged side action, wrong reply, route/payment hesitation, dirty object, or body residue rather than from person-plus-event labels such as `狗哥的婚礼`. The old-friend memory budget is also reduced to one cropped debt or object; a full warm proof-chain about how good the other person was tends to turn the refusal into a designed moral story. The checker now treats non-`日寄` titles containing high-signal topic words, including `婚礼`/`结婚`, as `题面诊断型标题` under generated draft-gate, while still keeping corpus strict calibration separate.

Follow-up controller review of `iteration-20260706-54/eval-09-2024-classmate-wedding` found a tool-flow/source-repair gap rather than a blind-round-ready sample. The title improved from the diagnostic event label to a side-action handle and the draft no longer invented a real project chain or concrete campus landmark, but the bounded file still stopped at preflight before checker call 1/2. The serious new failure was rhythm invalidation: the generator ran `rebalance_line_rhythm.py`, then overwrote `draft.md` with a fresh 11-line prose-block version, then called the wrapper without another rhythm repair. Clean-eval trace validation now flags this as `节奏脚本后重写未重跑节奏修复`, and Layer 0/clean-generation guidance now says content rebuilds after rhythm scripts must preserve the line-broken corridor or rerun the relevant script before the next wrapper call. This is a protocol/source-shape fix, not a pass claim.

Follow-up attempt `iteration-20260706-55/eval-09-2024-classmate-wedding` exposed a separate artifact-location failure before it could become quality evidence. The generator wrote the draft through an absolute `filePath` under `<skill-dir>/iteration-.../draft.md` instead of the external case workspace, then repeated the rhythm-script overwrite problem. The trace checker now treats only exact relative `draft.md` / `.\draft.md` writes as valid bounded artifacts, requires `Get-Location` / `pwd` before the first clean-eval write, rejects a current directory that is the distributable skill directory, and still flags stale rhythm repair after a later rewrite. Runtime guidance now states that the write/file tool path argument must be exactly `draft.md` or `.\draft.md`; `<skill-dir>` is only for bundled references and scripts. This fixes test integrity and portability; it is not a blind-round pass claim.

Follow-up retest `iteration-20260706-56/eval-09-2024-classmate-wedding` used `deepseek/deepseek-v4-pro`, `--variant high`, and thinking enabled from the external case directory after commit `075a3c4`. The trace was clean except visible thinking chatter, and the draft was written to the correct external workspace, so the path fix worked. The bounded run still stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`), with hard findings `段落发动机信号偏弱` and `社交拒绝室内冷感过密`; style-profile was `revise` with red line-rhythm, n-gram, and punctuation families. A copied finalized repair using `deepseek/deepseek-v4-flash` cleared strict hard errors but remained style-profile `review` with seven yellow drift families, so the diagnosis is `systemic_gap`, not blind-round-ready. The practical root cause is broader than cold-room filler: social-decline prompts still let body/screen/route/object texture carry the article while the refusal aftermath does not change enough action. Source guidance now requires a post-refusal consequence before drafting and a private deletion test: if the article stops moving after deleting all room texture except one object, rebuild from the reply aftermath. The checker now flags `社交拒绝纹理替代后果不足` in generated draft-gate mode.

Follow-up retest `iteration-20260706-58/eval-09-2024-classmate-wedding` used `mimo/mimo-v2.5-pro`, `--variant high`, and thinking enabled after the path-trace fix. Trace validation now correctly accepts absolute display titles from file tools while still rejecting an absolute `INPUT.filePath` under the wrong workspace. The bounded run again stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`), and the draft still failed strict/source validation: it made `忙项目` into a true current work chain, used `充电线` as a neat title-open-tail symbolic closure, and solved the refusal with a polished courtesy reversal (`狗哥` sending a small red packet with `心意到了` / `项目忙就算了`). A copied finalized repair accidentally invoked clean-run state inside `finalized/` and still remained profile `revise`, so the diagnosis stays `systemic_gap`, not blind-round-ready. Root fix from this case: Layer 0/1 now treats post-refusal red-packet kindness, `沾点喜气`, `心意到了`, and `项目忙就算了` as generated-draft courtesy reversals, and source guidance now tells the generator to prefer plainer consequences such as silence, one short reply, address/photo pressure, payment/route/body trouble, unfinished transfer, or an old debt that changes the narrator's hand. The same pass also marks side-object overuse as a source failure when one object carries title, opening, and ending. Corpus strict calibration after this rule still reports zero hard errors across the 38 originals.

Follow-up retest `iteration-20260706-59/eval-09-2024-classmate-wedding` used `opencode-go/minimax-m3`, `--variant high`, and thinking enabled. The run did not produce a reviewable article: the model fell into visible planning/counting, wrote no `draft.md`, and never called `clean_run_checker.py`. This is a source/tool-flow failure, not a style-quality sample. It suggests that clean-eval instructions still risk making some models count lines and characters instead of first persisting a complete titled article.

Follow-up retest `iteration-20260706-60/eval-09-2024-classmate-wedding` used `deepseek/deepseek-v4-flash`, `--variant high`, and thinking enabled. The bounded run wrote `draft.md` and respected the preflight stop boundary, but it stopped before real checker call 1/2 (`calls=0`, `preflights=3`). The bounded draft still invented an unsupported current-work consequence for the excuse, lacked rough self-damage and paragraph-engine signals, and style-profile stayed `revise` with red connector, line-rhythm, and punctuation families. A separate finalized repair using `mimo/mimo-v2.5-pro` improved the courtesy-reversal issue but exposed a new systemic failure: the wedding refusal was resolved inside a private old-debt transfer loop (`支付宝`, `备注`, `未领取`, `退回`, `已读但钱没收`) rather than through a visible reply, route/payment/body/action consequence. Source guidance now treats that as a false consequence: old-debt transfer may appear, but only if it changes a reply, doorway/route/payment/body action, or social position; otherwise it should be cut. The checker now reports `社交拒绝私密转账假后果` under generated draft-gate. Regression verification also confirmed the rule is not excused by earlier unrelated texture or memory words such as `漏网`, `快递`, `他问`, or `他看`.

Calibration note: original-corpus placebo checks must not use `--draft-gate`, because generated-draft gates intentionally reject patterns that originals may naturally contain. Current strict non-draft-gate calibration over the 38 originals reports zero hard errors; generated-draft gates remain reserved for new test drafts and finalized repairs.

Follow-up retest `iteration-20260706-61/eval-09-2024-classmate-wedding` used `deepseek/deepseek-v4-pro`, `--variant high`, and thinking enabled after commit `dfee1df`. This was the first recent social-decline run to reach the actual two-call clean-eval boundary (`calls=2`, `preflights=1`) with zero strict hard errors and no trace hard errors. It is still not blind-round-ready: bounded style-profile remained `review` with red punctuation and yellow cognitive/connector/ngram/texture drift. The draft improved by avoiding invented work facts, diagnostic wedding title, courtesy red-packet closure, and private transfer loop, but it over-relied on screen/body/room surfaces and made the refusal legible through too many designed cognition hooks. The finalized repair attempt used `mimo/mimo-v2.5`, timed out after repeated ending/metric repair, and failed formal validation: it ran ordinary `--strict` during repair, missed formal `--strict --draft-gate --genre standard`, and the controller later found `strict: 高频词覆盖不足` plus style-profile `review` with red `ngram_texture`. Source/repair fix from this case: finalized repair mode now locks the formal hard-gate command up front; a plain strict checker run is only a smoke check, never a pass. Connector-thin drafts must be repaired by adding or reshaping a real scene turn such as wrong reply, payment handoff, body interruption, route change, or object-forced action, not by sprinkling connector words. Trace validation was also corrected so wrapper-internal rhythm repairs are not misreported as stale rhythm-script violations.

Follow-up retest `iteration-20260706-62/eval-03-2023-valentines-alone` used `mimo/mimo-v2.5-pro`, `--variant high`, and thinking enabled. The first command attempt failed because of shell redirection, then the formal rerun completed. The bounded draft stopped at preflight before checker call 1/2 (`calls=0`, `preflights=3`) and trace validation found a real protocol error: the generator ran `soften_line_endings.py`, then edited `draft.md` for rough self-damage, then called the wrapper without rerunning the rhythm script. Bounded hard gate still had 3 errors (`高频词覆盖不足`, `粗粝自毁信号不足`, `逗号密度过高`) and style-profile remained `review` with red line-rhythm/punctuation. The separate finalized repair with `deepseek/deepseek-v4-flash` cleared strict hard errors, but style-profile remained `review` with red line-rhythm/punctuation and yellow cognitive/structure/texture/title drift; diagnosis is `systemic_gap`, not blind-ready. Manual audit: the source shape was a sealed private holiday story (`朋友圈礼物 -> 外卖 -> 香菜 -> 洗碗 -> 床`), and finalized repair retitled to `明天再说吧`, creating a clean title-tail contract. Source/repair fixes from this case: private holiday/romance-feed/takeout prompts now require a side engine outside the feed-food-bed chain; prompt props must not become title, opening, and ending at once; clean-eval preflight now tells generators to make content repairs first and run `soften_line_endings.py` last before the next wrapper call.

Latest source/repair update after `iteration-20260705-26`: cross-year, annual-summary, social-feed, and old-chat prompts must not only avoid screen archaeology; they need a positive present-day engine. One old record can trigger the article, but it must cause a wrong reply, outside interruption, practical chore, body noise, route/payment problem, wrong slipper, leaking package, or other low consequence before the first draft is saved. The checker now also flags private old-record archive chains in generated draft-gate mode and recognizes practical misread/wrong-slipper/door-threshold consequences as paragraph-engine signals. Titles such as `2024日寄`, `新年日寄`, `跨年日寄`, or `年度总结日寄` remain diagnostic calendar labels unless the body earns them as a side handle. Finalized repair still must persist to `finalized/draft.md`, clear strict hard gate, and pass style-profile rather than staying at `review`.

Follow-up retest `iteration-20260705-27/eval-08-2024-gout-flareup` used `deepseek/deepseek-v4-flash`, `--variant high`, and thinking enabled after commit `7e3ebef`. The bounded run correctly checked `.anlin-clean-eval-mode`, used `clean_run_checker.py`, and respected the actual `CLEAN_RUN_PREFLIGHT_STOP` boundary, but stopped before formal checker call 1/2 (`calls=0`, `preflights=3`). A trace-controller false positive initially reported stop-boundary escape because it treated the reference text mentioning `CLEAN_RUN_PREFLIGHT_STOP` as an actual wrapper output; `check_clean_eval_trace.py` now only treats standalone checker output lines beginning with the stop token as real stops. The copied finalized repair cleared strict hard errors, but style-profile remained `review` with red `ngram_texture` and `punctuation` families plus yellow cognitive/line-rhythm/structure/texture drift, so the case is still `systemic_gap` and not blind-round-ready. Root fixes from this case: illness/body prompts now have a source-level guard against private case-report chains, searched `有人说...` claims must become one cropped screen surface, and repeated symptom/food/screen packets should be cut before adding new roughness.

Follow-up retest `iteration-20260705-28/eval-08-2024-gout-flareup` used another rotated model surface with thinking enabled after commit `4d6c667`. The bounded run reached the real clean-eval checker boundary (`calls=2`, `preflights=2`) but the saved draft still had severe source failures: illness pressure became a current-work chain (`领导`, `周一`, `请假`, `扣钱`), the ambient motif repeated too directly, illness/body/screen packets stayed too dense, and style-profile remained `revise` with red connector, ngram-texture, and punctuation families. The trace checker also produced a second false positive by treating post-stop todo text such as `[✓] Write draft.md` as an actual write action. Root fixes from this case: stop-boundary trace validation now only counts real write/edit tool lines, source guidance forbids turning practical body consequences into unsupported current-work identity, and the hard checker now flags unsupported work-consequence chains plus repeated ambient refrains. These fixes are model-agnostic; model rotation is used only to expose failure modes.

Follow-up retest `iteration-20260705-29/eval-08-2024-gout-flareup` used `deepseek/deepseek-v4-pro`, `--variant high`, and thinking enabled after commit `cbe46b4`. The bounded run had a clean trace except visible thinking chatter, but stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`) with illness over-density, missing breathing point, connector glue, and style-profile `revise` red families. A copied finalized repair initially looked acceptable under style-profile (`yellow`, `checkpoint_decision=pass`), but manual audit found a validator loophole: the title `半瓶可乐` did not contain `日寄`, so the strict checker failed to apply standard-diary length/rhythm gates to a 789-character standard diary. The checker now treats non-`日寄` titles as standard-diary gate targets when the body is a standard diary attempt and has no short-form signal; regression tests cover both 650-899-character drafts and very short 300+ character attempts. Revalidation now rejects that finalized draft with `strict: 标准日寄完整文章篇幅缓冲不足`, so this case is not blind-round-ready.

Follow-up retest `iteration-20260705-30/eval-07-2024-mothers-day-sincere` used `mimo/mimo-v2.5`, `--variant high`, and thinking enabled. The bounded draft avoided trace hard errors but stayed unresolved: the wrapper stopped at preflight, hard findings included comment-chain formulae, an AI binary reframe, thin connector coverage, family-theme over-density, weak rough/self-damage and paragraph engine signals, and style-profile `revise` with red line-rhythm, ngram-texture, and punctuation families. The copied finalized attempt timed out and left `finalized/draft.md` unchanged, so this is an artifact failure plus `systemic_gap`. Root fix from this case: genre routing must happen before standard-diary length/rhythm gates. Mother's Day/direct-memory prompts can route to sincere mode with matched short-form evaluation, while ordinary family pressure or a non-`日寄` title must not escape standard-diary gates.

Follow-up retest `iteration-20260705-31/eval-07-2024-mothers-day-sincere` used `mimo/mimo-v2.5-pro`, `--variant high`, and thinking enabled after the first genre-routing fix. The trace had only visible-thinking warning, but the wrapper still stopped at preflight before call 1/2 because `clean_run_checker.py` applied standard-diary preflight thresholds to a sincere mother-memory draft. Rechecking the same draft after the fix shows the standard length/rhythm/roughness findings disappear; only non-blocking short-genre theme concentration remains, plus the historical stop-state marker from the old bounded run. Root fix from this case: short-genre preflight is now separated from standard diary preflight, and non-standard theme concentration no longer becomes the standard `单主题词密度偏高` draft-gate error.

Follow-up retest `iteration-20260705-32/eval-07-2024-mothers-day-sincere` used `deepseek/deepseek-v4-pro`, `--variant high`, and thinking enabled. The bounded run reached the real clean-eval checker boundary (`calls=2`, `preflights=1`) and hard-checker findings were reduced to non-blocking short-genre warnings plus the expected historical stop marker if rechecked after the boundary. The remaining validation issue was style-profile scope: `--genre sincere` fell back to global priors because only two sincere originals are in the bundled corpus profile. `scripts/check_style_profile.py` now reports such non-standard global fallback as `inconclusive`, `formal_gate: not_pass`, and `checkpoint_pass=false` under strict formal audit, while `scripts/summarize_dev_checkpoints.py` records it as `review`, not `pass`, so development reports cannot treat insufficient genre calibration as blind-round readiness. This is a validation-scope correction, not a claim that the article passed blind review.

Copied finalized repair for the same case used `opencode-go/minimax-m3`, `--variant high`, and thinking enabled. It cleared the strict hard checker, but profile validation remained inconclusive and controller summary classified the result as `repair_path_gap`, not blind-ready. Manual source review found the real issue: the short sincere draft had become a clean, linear, polished essay with low line variance, no longer clumsy lines, and too little awkward practical interruption. Source-layer fix: `SKILL.md`, `clean-generation-brief.md`, `runtime-brief.md`, `generation-modes.md`, `feature-budget.md`, and `runtime-layer-map.md` now give short sincere/micro-hope pieces a model-agnostic breathing corridor instead of relying on external cleanup or standard-diary ratios.

Follow-up retest `iteration-20260705-33/eval-07-2024-mothers-day-sincere` used `gateway/gpt-5.5`, `--variant high`, and thinking enabled after commit `d7a0a38`. The bounded run reached the real two-call checker boundary (`calls=2`, `preflights=2`) with a clean trace except visible provider thinking chatter. The first submission still failed, but the bounded final had zero strict hard errors; style-profile remained inconclusive for `--genre sincere` because the sincere stratum has only two originals, with punctuation still red in the global fallback. A copied finalized repair used `deepseek/deepseek-v4-flash`, `--variant high`, and thinking enabled; it cleared strict hard errors and removed red profile families under global fallback, but the genre-matched profile still reported `profile_inconclusive_fallback`, so controller summary remains `repair_path_gap` / not blind-round-ready. Root evidence: the short-genre breathing corridor helped avoid the previous polished-minimalism hard failure, but short sincere validation now needs matched-original blind/placebo calibration rather than global standard-diary ratios.

Follow-up validation tooling update after `iteration-20260705-33`: `prepare_blind_test.py` and `run_blind_test.py` now accept `--match-genre`. Both impostor and placebo rounds use the generated draft as a hidden matching anchor, prefer same-genre originals with similar length/line shape, then backfill nearest samples when the small corpus lacks enough exact matches. `mapping.json` records each sample's inferred genre and match diagnostics. This is a model-agnostic validation fix for short sincere/micro-hope/surreal cases and should be used before judging the finalized short sincere draft; it does not change generation prompts or add model-specific branches.

Matched short-sincere blind package `blind-rounds-matched-after-conservative-genre` then ran `3 impostor + 1 placebo` with titles retained, neutral judge directories, and rotated judge models (`deepseek/deepseek-v4-pro`, `mimo/mimo-v2.5-pro`, `opencode-go/minimax-m3`, `gateway/gpt-5.5`). Result: generated-sample stable identification was `1/3 = 33.3%`; stable placebo false accusation was `1/1 = 100%`; two impostor rounds stably accused original samples instead of the generated sample. This does not meet the target and also shows judge over-sensitivity. The useful failure signal is still source-level: short sincere drafts remain prone to clean literary-story shape, high sensory/object density, linear mother-memory arcs, and overly engineered endings. Fix this by changing generation/revision guidance and checker warnings, not by model-specific branches.

Architecture update after that blind review: Layer 0/1 now names short sincere literary-story closure as a source failure distinct from shortness or polished minimalism. The generator should break the title-object / mother-memory / childhood-rain / withheld-message / final-residue proof-chain before first `draft.md`, not stretch the piece into standard diary length and not add background/game/body packets as filler. The checker and clean-eval preflight now emit `短真诚小小说闭合` / `short_genre_literary_story_closure` as a warning/shape stop so the repair changes the source arc. This is a failure-discovery improvement, not a target pass claim.

Follow-up retest `iteration-20260706-34/eval-07-2024-mothers-day-sincere` used `deepseek/deepseek-v4-pro`, `--variant high`, and thinking enabled after commit `b316c83`. The bounded clean-eval run checked the marker correctly and respected the preflight stop boundary, but stopped before formal checker call 1/2 (`calls=0`, `preflights=3`). The final bounded draft was only 356 body Chinese characters, had no longer clumsy lines, and still kept the Mother's Day / no-message / eggs / childhood-rain chain. A copied finalized repair using `mimo/mimo-v2.5-pro` left the article at `review`: zero strict hard errors, but `短真诚小小说闭合`, `短体裁连接信号偏少`, red global fallback line-rhythm/punctuation drift, and style-profile `profile_inconclusive_fallback`. Diagnosis is `systemic_gap`, not blind-round-ready. Root fix from this case: short sincere guidance now explicitly rejects 250-500-character sketches, requires a practical completeness floor with longer clumsy lines, and tells generators to preserve one family-memory prompt prop, crop one trace, and discard or bury the rest rather than visibly keeping every supplied object, message, and memory.

Follow-up retest `iteration-20260706-35/eval-07-2024-mothers-day-sincere` used `mimo/mimo-v2.5`, `--variant high`, and thinking enabled after commit `e263478`. The bounded run still stopped at preflight before checker call 1/2 (`calls=0`, `preflights=3`), but the failure moved: the draft was no longer a 250-500-character sketch. It became a 678-character, 16-line smooth paragraph-shaped sincere piece titled `五月十二日`, kept too many Mother's Day prompt props, and used a typed-then-deleted ending loop. Controller recheck found `short_genre_literary_story_closure`, prompt-chain completeness, line-final comma problems, and global-fallback line-rhythm/ngram/punctuation drift. Diagnosis remains `systemic_gap`, not blind-ready. Current source/tooling fix: Layer 0/1 now treats 520+ short sincere bodies with 8-18 smooth complete-sentence lines as `prose-block compression`, blocks date/holiday titles for direct holiday prompts, and tells the generator to choose a side-action title while cutting one full prop family before `draft.md`.

Follow-up retest `iteration-20260706-36/eval-07-2024-mothers-day-sincere` used `opencode-go/minimax-m3`, `--variant high`, and thinking enabled after commit `8e7262f`. The bounded run reached the real clean-eval two-call boundary (`calls=2`, `preflights=2`) and had zero strict hard errors, but style-profile remained `review` with red global-fallback `line_rhythm` and `punctuation`. A copied finalized repair used `deepseek/deepseek-v4-flash` and wrote back to `finalized/draft.md`, but manual audit found a new repair-path failure: it added delivery, branded food, gift/media/video, variety-show, route/game-like, and background-adjacent packets to manufacture thickness and long lines. This is not a blind-round-ready result; it shows that short sincere repair can become an "expanded proof essay" even after hard gates are clean. Current source/tooling fix: short sincere/micro-hope guidance now forbids repair stuffing as a source move, `check_anlin_violations.py` and `clean_run_checker.py` warn on overlong short-genre drafts that import multiple unrelated new material families, and `summarize_dev_checkpoints.py` now infers style-profile genre from eval case names when `--genre` is omitted while still preferring explicit `--genre` in formal reports.

Follow-up retest `iteration-20260706-37/eval-07-2024-mothers-day-sincere` used `deepseek/deepseek-v4-pro`, `--variant high`, and thinking enabled after commit `f89f264`. The bounded run stopped at preflight before checker call 1/2 (`calls=0`, `preflights=3`): zero strict hard errors, but the 512-character draft had no longer clumsy lines and kept the egg / childhood rain / no-message / final-residue proof chain. Targeted smoke after this source update now adds `short_genre_present_action_anchor` to the preflight diagnosis for that draft. Root fix from this case: short sincere family prompts must choose a current practical anchor before selecting memory. Eggs, rain, holiday feed, and blessing are pressure props; they are not anchors unless the present action around them fails and changes the next move. This is a source-level improvement and not a blind-round pass claim; finalized repair for this specific case still needs to be run in a separate `finalized/` directory.

Controller re-summary of `iteration-20260706-37/eval-07-2024-mothers-day-sincere` after the finalized artifact was inspected classified the case as `systemic_gap`: bounded clean-eval is `invalid` because it stopped at preflight before checker call 1/2, and the copied finalized repair is now `fail` under strict hard gate plus sincere style-profile fallback. The bounded checkpoint has 4 hard errors and finalized has 3 hard errors after the stricter generated-draft gate; both preserve the same `egg/rain/holiday/no-message/final residue` spine, so this is not blind-round-ready. Current source/tooling fix: in generated `--draft-gate` mode, short sincere complete-article, polished-prose, repair-stuffing, missing-present-anchor, and small-story-closure findings are now blocking errors, and runtime guidance now treats missing present-action anchor as a source reset: retitle from the present action, start with a practical failure, keep at most one old memory prop as a cropped leak, and do not locally patch the old proof chain.

Follow-up retest `iteration-20260706-38/eval-07-2024-mothers-day-sincere` used `mimo/mimo-v2.5`, `--variant high`, and thinking enabled after commit `6362a4f`. The bounded run still stopped at preflight before checker call 1/2 (`calls=0`, `preflights=3`). The final bounded draft improved by starting from washing dishes, oil, and cold water, but it still had no standalone title, no longer clumsy lines, and quickly handed control to the mother/egg/rain/holiday/no-message proof chain. Controller hard gate reported `strict: 缺少标题`, `strict: 短体裁完整度不足`, and `strict: 短真诚小小说闭合`; style-profile was inconclusive for sincere fallback with red global line-rhythm and punctuation families. A trace finding for pre-draft `anti-ai-slop.md` was diagnosed as a controller false positive caused by scanning dumped skill body text inside mixed OpenCode logs. Current source/tooling fix: clean-eval preflight now flags missing titles and `short_genre_prompt_prop_too_early`, the strict generated-draft gate promotes `短真诚题面物件过早`, and Layer 0/1 guidance now requires the first 8-12 body lines of short sincere family prompts to let a current practical action fail before one memory/screen prop leaks in. This remains a source-guidance failure, not blind-round-ready.

Follow-up retest `iteration-20260706-39/eval-07-2024-mothers-day-sincere` used `deepseek/deepseek-v4-flash`, `--variant high`, and thinking enabled from the skill state around commit `9e38d04`. The bounded run again stopped at preflight before checker call 1/2 (`calls=0`, `preflights=3`), so it is not blind-round-ready. It did improve the opening source order: the title existed, the first practical action held longer, and the prompt props did not take over immediately. The remaining failure changed shape: the draft became a 560-character, 74-line poem-shaped short sincere grid with zero longer clumsy lines, weak paragraph engine, and a still-linear mother/egg/rain/title-residue small-story closure. Post-run controller recheck after the short-corridor update routes this draft as `sincere` rather than standard diary and reports `strict: 短体裁完整度不足` plus `strict: 短真诚小小说闭合`, not a mistaken standard 900-character failure. Current source/tooling fix: short sincere complete-article guidance now says formal clean-eval usually needs about 650-850 body Chinese characters, about 28-55 body lines, 4-7 uneven clusters, and several longer clumsy action/memory/reply lines; 520-649 characters or 55+ mostly tiny rows is a risk buffer, not a safe default. The checker and clean-run preflight now recognize `五月十二/5月12 + mother + care-memory` as sincere and flag short sincere tiny-row grids before formal checker calls.

Follow-up retest `iteration-20260706-40/eval-07-2024-mothers-day-sincere` used `mimo/mimo-v2.5-pro`, `--variant high`, and thinking enabled after commit `b694b35`. The bounded run still stopped at preflight before checker call 1/2 (`calls=0`, `preflights=3`), so it is not blind-round-ready. The new short sincere corridor changed the failure again: instead of a tiny-row grid, the generator saved an 8-line prose-block essay at 541 body Chinese characters. The wrapper correctly flagged `short_genre_complete_article_buffer` and `short_genre_prose_block_compression`; controller hard gate also found prompt-chain completeness, literary hovering ending, and zero line-final comma rhythm. Root fix from this case: clean-eval short-genre preflight now blocks fewer than 24 actual body rows with `short_genre_body_lines`, and the repair text tells the generator to rebuild the visible page into roughly 28-55 body lines across 4-7 clusters instead of adding one more paragraph. This is still a source-guidance improvement, not a pass claim.

Follow-up retest `iteration-20260706-41/eval-07-2024-mothers-day-sincere` used `deepseek/deepseek-v4-pro`, `--variant high`, and thinking enabled after commit `f935352`. The bounded run checked the clean-eval marker, loaded only the clean-generation brief before the first draft, and respected `CLEAN_RUN_PREFLIGHT_STOP`, but still stopped before formal checker call 1/2 (`calls=0`, `preflights=3`). The first meaningful improvement was that the draft began from a current sink/neighbor action rather than immediately from the holiday. The remaining source failure was more precise: the generator treated "brief" family props as safe and preserved no-message + eggs/plastic-bag + childhood rain/raincoat as the same designed proof chain. Controller summary classified the bounded result as `invalid` with hard errors for short-genre completeness, short sincere small-story closure, and line-final comma rhythm. A separate finalized repair attempt with `opencode-go/minimax-m3` first failed by trying to read parent controller files; a narrowed retry wrote back to a separate `finalized-repair2/draft.md` and cleared strict hard-gate errors, but controller summary still reported finalized `review` because the sincere stratum falls back to global profile, punctuation remains red under that fallback, and the final article is not blind-round-ready. Root fix from this case: short sincere guidance now defines `cropped trace` more strictly. A one-line childhood-rain/raincoat/school-arrival memory is still a full memory family, not a cropped trace; a one-line eggs/plastic-bag/home-trip handoff is still a full object-memory family. The source layer and wrapper hints now tell the generator to cut one proof family completely, and the repair layer warns against making a short sincere article look repaired by sampling `其实/觉得/发现/好像/不过/突然/于是` once each.

Follow-up retest `iteration-20260706-42/eval-07-2024-mothers-day-sincere` used `mimo/mimo-v2.5`, `--variant high`, and thinking enabled after commit `b631509`. This did not produce a reviewable article. The generator checked the clean-eval marker and read the clean-generation brief, but then wrote repeated scratch prose and process reasoning to the terminal instead of persisting `draft.md`; it never called `clean_run_checker.py`. The controller now records this as `invalid` with `clean_stop_reason=missing_draft`, not as a style failure or blind-round candidate. Root fix from this case: `check_clean_eval_trace.py` now distinguishes actual checker/draft-write tool actions from references that merely mention `clean_run_checker.py` or `Write draft.md`, and `summarize_dev_checkpoints.py` now emits a JSON/Markdown invalid report plus `controller-audit/bounded-draft-missing.md` when no draft artifact exists. Runtime guidance now states the clean-eval tool order explicitly: marker check -> load `clean-generation-brief.md` -> write one complete `draft.md` -> run `clean_run_checker.py`. This is a protocol/source-flow repair, not a pass claim.

Follow-up retest `iteration-20260706-43/eval-07-2024-mothers-day-sincere` used `deepseek/deepseek-v4-flash`, `--variant high`, and thinking enabled after commit `3fb8470`. This also did not produce a reviewable article. The generator checked `.anlin-clean-eval-mode`, then tried to glob/search a parent skills directory to rediscover `anlin-writing`; the runtime rejected that external directory access, and no `draft.md` or checker call followed. Controller classification is `invalid` with `clean_stop_reason=missing_draft`, trace errors for missing `draft.md` and missing `clean_run_checker.py`, and no blind-round readiness. Root fix from this case: runtime guidance now has a skill-discovery stop rule. Once `anlin-writing` has triggered, the generator must not search parent or sibling skill directories; if a bundled reference cannot be resolved, it should fall back to the already-loaded Clean-Eval Minimum, persist a complete titled `draft.md`, and let the controller validate or fail the artifact.

Follow-up retest `iteration-20260706-44/eval-07-2024-mothers-day-sincere` used `mimo/mimo-v2.5-pro`, `--variant high`, and thinking enabled after commit `6c4159a`. The source-discovery fix worked in the narrow sense: the generator did not search a parent skills directory. It still wrote the article to a non-relative path under `<skill-dir>` instead of the active case workspace, so the controller case had no `draft.md` and the run is `invalid` with `clean_stop_reason=missing_draft`. The mislocated draft also stopped at preflight and still showed short sincere prose-block/closure problems, so it is not a blind-round candidate. Root fix from this case: runtime guidance now separates `<skill-dir>` from the output directory more explicitly, and trace validation flags any clean-eval article write path other than relative `draft.md` / `.\draft.md` as `clean-eval写稿路径不是相对draft.md`.

Follow-up audit on `iteration-20260705-26/eval-06-2024-new-year-nostalgia` with the updated checker showed the copied finalized draft no longer has strict hard-gate errors; the earlier `段落发动机信号偏弱` finding was partly a checker undercount. It still fails the finalized checkpoint because style-profile remains `review`, mainly from high body/object/screen texture density, high concrete-entry count, and punctuation/structure drift. The source lesson is not "add more rough detail"; it is to reduce texture-as-repair and make old-record pressure produce one present social/practical consequence earlier.

Follow-up retest `iteration-20260705-25/eval-06-2024-new-year-nostalgia` used a rotated available model surface after commit `fcdf374`. The bounded run respected the clean-eval stop boundary but stopped at preflight (`calls=0`, `preflights=3`) with title, unsupported-place, connector-spread, roughness, paragraph-engine, high-risk convenience-store, and quiet-explanation issues; style-profile was `revise` with red connector, line-rhythm, ngram-texture, and punctuation families. The finalized repair failed as an artifact: the agent printed a long final version and process analysis in the log but left `finalized/draft.md` unchanged. Diagnosis is `systemic_gap`, not blind-round-ready. Root fixes from this case: source guidance now blocks calendar-label titles and screen-archaeology chains for annual-summary/old-chat prompts, and controller summary now marks unchanged finalized artifacts invalid when bounded did not pass.

Follow-up retest `iteration-20260705-24/eval-03-2023-valentines-alone` used a randomly selected available model surface after commit `f8fd7cf`. The bounded run respected the clean-eval stop boundary but stopped at preflight (`calls=0`, `preflights=3`) with hard errors for thin connector coverage, weak rough self-damage, and weak paragraph engine; style-profile was `review` with red punctuation and six yellow families. A copied finalized repair then passed strict hard gate and style-profile (`yellow`, zero red families, `checkpoint_decision=pass`) with low corpus overlap. Diagnosis is `source_guidance_gap`: ordinary repair can converge, but first-draft guidance still let a quiet room/phone/food article stay too private and period-heavy. Root fix from this case: Layer 0/Layer 1 now asks quiet interior prompts to pick one outside contact or practical handoff that leaves residue, rather than relying on mirror/body observation or another person's glance.

Follow-up retest `iteration-20260705-23/eval-14-blended-afternoon-festival-forbid-money-portable` used a rotated available model surface after commit `7621bee`. The bounded run respected the clean-eval stop boundary but stopped at preflight (`calls=0`, `preflights=3`). Bounded hard errors included comment-chain formulae, insufficient connector spread, weak rough self-damage, and weak paragraph engine; style-profile was `review` with red line-rhythm and punctuation families. A copied finalized repair timed out after metric-chasing edits, so it was stopped; the bounded draft hash stayed unchanged. The best saved finalized draft had zero strict hard errors but style-profile remained `review` with `checkpoint_decision=not_pass_review_required`, and manual prompt-compliance review found a blocking violation: the prompt said not to write money/consumption/price, but the draft kept a store/water purchase chain. Root fixes from this case: source guidance now treats explicit negative constraints as hard pre-draft gates, social-feed/group material still needs screen-surface conversion before drafting, and repair guidance now blocks short-line-count chasing as a substitute for source rhythm.

Follow-up retest `iteration-20260705-22/eval-10-absurd-coincidence-projection-after-texture-repair-gate` used a realistic vendor/coincidence prompt after commit `dc4306d`. The bounded trace was not hard-contaminated, but the run stopped at preflight before checker call 1/2 (`calls=0`, `preflights=3`), with hard errors for weak rough self-damage and weak paragraph engine and style-profile `review`/red connector and punctuation drift. A copied finalized repair then cleared strict hard errors and had zero red profile families, but style-profile remained `review` because yellow drift families accumulated across cognitive mechanism, connectors, punctuation, structure, and texture. Diagnosis is `systemic_gap`, not blind-round-ready. Root fix from this case: style-profile reports now expose `checkpoint_decision`, and `review` is explicitly not a finalized checkpoint pass even when red families are empty; source/repair guidance should thin overcorrected body/material/cognition/off-axis density instead of adding more texture.

Additional architecture audit found a source-load conflict: `SKILL.md` said clean-eval generation should begin with only `clean-generation-brief.md`, while the workflow and `runtime-brief.md` still implied loading the longer runtime brief before drafting. The clean-eval first-draft path now has a single source loop in `clean-generation-brief.md`: friction -> one pressure surface -> off-axis consequence -> fact gate -> complete titled draft -> bounded checker repair. `runtime-brief.md`, `generation-modes.md`, and `anti-ai-slop.md` remain available for ordinary drafting, analysis, or post-checker repair, not as mandatory pre-draft material.

Clean-eval preflight now blocks obvious non-article drafts before they consume a formal checker call: too short, too overfilled, fewer than 45 body lines, prose-block compression, missing connector/engine/rough self-damage signals, or high-risk AI/background surfaces. Preflight is bounded; if it prints `CLEAN_RUN_PREFLIGHT_STOP`, the generation run should stop and the external controller should mark it invalid or failed instead of letting the generator repair indefinitely.

Development validation now uses two checkpoints per serious case. The bounded checkpoint records the fresh-agent result after clean-eval limits, including bounded preflight and limited checker-driven repair; it also records stage snapshots for the first submitted draft, checker-call submissions, and bounded final. The finalized checkpoint records the result after ordinary multi-round repair from a separate copied draft. The finalized checkpoint is not allowed to pass on the normal checker alone; it must clear strict hard-gate validation and the bundled style-profile audit when the profile is available. A bounded failure with a finalized pass means source guidance should be strengthened; bounded failure with finalized `review/fail/invalid` means the final article is still unresolved and diagnosis must broaden across source guidance, repair references, fact gates, profile assumptions, and checker behavior.

Latest source-level tightening: clean-eval first draft may only use `clean-generation-brief.md` plus phase/date handling when needed. Repair, title, structure, anti-slop, corpus-card, judge, and ratio references are explicitly post-draft materials for this mode.

Latest controller tooling update: `scripts/summarize_dev_checkpoints.py` now summarizes bounded and finalized checkpoints separately, copies drafts and clean-run stage snapshots into an external `controller-audit/` directory before normal checker runs, classifies the development result as source guidance gap, repair path gap, systemic gap, or ready for blind rounds, and reports `blind_round_readiness`. The script exits nonzero for any state that is not ready for blind rounds; the JSON/Markdown report is still the evidence to read.

Latest development case after that update: `2022-food-delivery-heatstroke` was run as a bounded clean-eval case with a realistic prompt. The generated run was marked invalid because it read the correct skill but called the normal checker instead of `clean_run_checker.py`. The finalized repair checkpoint also did not pass strict/profile validation. Root fixes from that case: the checker route is now mode-first rather than normal-checker-first, and delivery prompts now explicitly forbid drifting into an unsupported married-rider biography.

Latest two-checkpoint retest after `e80e3d2`: `iteration-20260704-10/eval-02-food-delivery-heatstroke` used the realistic prompt with `longcat/LongCat-2.0`. Trace audit was clean, but bounded clean-eval stopped at preflight (`calls=0`, `preflights=3`) because the draft became a 148-line no-punctuation short-line grid with binary reframe residue. Finalized repair improved the binary reframe and line count but still failed strict hard gate (`粗粝自毁信号不足`, `段落发动机信号偏弱`, `行末逗号比例`) and style-profile stayed `review`, so the controller diagnosis is `systemic_gap`, not source-guidance-only. Root fixes from this case: preflight now flags overfragmented grids directly, runtime guidance distinguishes line-final commas from internal comma chains, and repair guidance requires semantic reread after mechanical merging.

Follow-up retest `iteration-20260704-11/eval-02-food-delivery-heatstroke` triggered the correct skill but wrote `draft.md` before checking `.anlin-clean-eval-mode` and then used the normal checker instead of `clean_run_checker.py`; trace checker marked the bounded run invalid. Root fix: the entry contract and clean generation brief now require a lightweight marker check as the first tool action before any `draft.md` write or checker command.

Follow-up retest `iteration-20260704-12/eval-02-food-delivery-heatstroke` correctly checked the clean-eval marker first and used `clean_run_checker.py`, but still stopped at bounded preflight (`calls=0`, `preflights=3`). Controller audit exposed two tooling issues that could distort diagnosis: `矿泉水` was falsely matched as unsupported game `泉水`, and real low-body/status material such as exposed toes, stomach trouble, and falling/being mistaken for碰瓷 was not counted as rough self-damage. Those checker issues are now covered by regression tests; the run itself still does not prove readiness for blind rounds.

Follow-up retest `iteration-20260704-13/eval-02-food-delivery-heatstroke` produced a clearer two-checkpoint diagnosis: bounded clean-eval was invalid because the generator kept writing after `CLEAN_RUN_PREFLIGHT_STOP`, while finalized repair from a copied draft passed strict hard gate and style-profile validation. Root fix from that run: stop boundaries in `clean_run_checker.py` now return a successful process status while still recording invalid/stopped state for the controller. This reduces the chance that a generation agent treats a protocol stop as a command failure to repair.

Follow-up retest `iteration-20260704-14/eval-02-food-delivery-heatstroke` confirmed the stop-boundary fix: trace audit was clean and the generator stopped after `CLEAN_RUN_PREFLIGHT_STOP`. The remaining bounded failure was more specific: preflight blocked before the first actual checker call because the draft had four distinct connector signals instead of the full draft-gate target. The preflight threshold is now limited to severe connector starvation, so near-miss drafts can enter the first real checker pass and use the bounded repair opportunity. The same pass also clarified that style-profile `yellow` is acceptable for finalized checkpoint pass; do not keep rewriting only to remove yellow warnings.

Follow-up run `iteration-20260704-15/eval-02-food-delivery-heatstroke` did not produce a draft. The generator loaded the correct skill and checked the clean-eval marker first, but then attempted to write `draft.md` to a wrong date-stamped absolute path outside the assigned case directory, so OpenCode rejected the file write. This is a contaminated tooling/path run, not a prose-quality result. Root fix: runtime instructions now require relative `draft.md` / `.\draft.md` in the current task workspace and forbid invented absolute output paths.

Follow-up retest `iteration-20260704-16/eval-02-food-delivery-heatstroke` used the correct relative path and had a clean trace, but bounded clean-eval still stopped at preflight (`calls=0`, `preflights=3`). The draft stayed compressed at 30 body lines, had zero early line-final comma continuations, retained a binary reframe, and lacked a rough losing-face body/social consequence. Finalized repair cleared the strict hard gate but timed out while mechanically chasing punctuation; style-profile remained `revise` with red `line_rhythm`, `punctuation`, and `ngram_texture`. Diagnosis is `systemic_gap`. Root fixes from this case: preflight now names the exact rhythm scripts to run, clean generation explains breathing clusters instead of sentence rows, and finalized repair now requires a rhythm-reset rewrite rather than repeated percentage tweaking.

Follow-up run `iteration-20260704-17/eval-02-food-delivery-heatstroke` triggered the skill but did not enter clean-eval mode: it wrote before checking the marker and used the normal checker repeatedly, then ran `split_long_lines.py` directly. The run is invalid for bounded measurement. Root fix: the skill description now exposes the `.anlin-clean-eval-mode` marker and `clean_run_checker.py` route so the model sees the clean-eval hard entry even if it under-loads the full body.

Follow-up retest `iteration-20260704-18/eval-02-food-delivery-heatstroke` restored clean-eval routing: marker check happened first, the wrapper ran, and `split_long_lines.py` was used after the first preflight. The run still became invalid because the generator continued writing after `CLEAN_RUN_PREFLIGHT_STOP`, then hit `CLEAN_RUN_STOP`. Root fix: wrapper stop output and runtime docs now place `FINAL BOUNDARY`, `DO NOT WRITE draft.md`, and read-only next-action language at the start of the stop message.

Follow-up retest `iteration-20260704-23/eval-02-food-delivery-heatstroke` used the realistic prompt with `longcat/LongCat-2.0` after commit `4be07ac`. The bounded run again produced a complete titled article but was invalid: the generator wrote `draft.md` before checking `.anlin-clean-eval-mode` and used the normal checker instead of `clean_run_checker.py`. The copied `finalized/` repair then passed strict hard gate and style-profile validation (`yellow`, zero errors), so the controller diagnosis is `source_guidance_gap`: the final article can converge, but the first-draft/limited-checker path is still too weak. Root fix from this case: `clean-generation-brief.md` now puts the marker check at the top as the first tool action, and `check_clean_eval_trace.py` separately flags `clean-eval写稿前未检查模式标记` so future reports identify the entry failure directly.

Follow-up retest `iteration-20260704-24/eval-02-food-delivery-heatstroke` confirmed the marker/wrapper entry fix: the run produced `.anlin-clean-run-state.json` and stage snapshots. Bounded still failed before any actual checker call (`calls=0`, `preflights=3`) and then contaminated the run by writing after `CLEAN_RUN_PREFLIGHT_STOP`. The first-submission snapshot had hard failures for invented spouse identity, formulaic comment-chain/video reaction, connector starvation, and literary ending. Finalized repair cleared strict hard errors but remained style-profile `review` with red `line_rhythm`, so the diagnosis is `systemic_gap`. Root fixes from this case: `CLEAN_RUN_PREFLIGHT_STOP` no longer prints repairable preflight details to the generator, but stores them in state for controller diagnosis; the clean generation brief now explicitly scans for unsupported spouse/child identities in delivery prompts and treats rider videos with `有人说...` as comment-chain residue; finalized repair now treats `review` with red `line_rhythm` as unresolved, not acceptable `yellow`.

Follow-up retest `iteration-20260704-25/eval-02-food-delivery-heatstroke` used the realistic prompt with `longcat/LongCat-2.0`. The bounded trace was clean and respected the stop boundary, but the run stopped at preflight before any actual checker call (`calls=0`, `preflights=3`), so it is a source/preflight failure rather than evidence about the two real checker calls. The bounded final had no hard errors but style-profile stayed `revise` with red `connectors`, `line_rhythm`, and `punctuation`. Finalized repair then failed both strict hard gate (`高频词覆盖不足`, `散文块压缩`) and style-profile (`revise`, red `line_rhythm`, `ngram_texture`, `punctuation`) after compressing the draft into 36 body lines. Diagnosis is `systemic_gap`. Root fixes from this case: clean/finalized guidance now separates first-submission, preflight readiness, two-call checker boundary, and finalized convergence; `rebalance_line_rhythm.py` was added and wired into clean-run normalization; runtime docs now explicitly reject repairs that swing from short-line grid to 30-40-line prose blocks.

Follow-up retest `iteration-20260704-26/eval-02-food-delivery-heatstroke` used the realistic prompt with `longcat/LongCat-2.0` after commit `e722cc6`. The run correctly triggered `anlin-writing`, checked `.anlin-clean-eval-mode` before drafting, used `clean_run_checker.py`, and reached the actual two-call checker boundary (`calls=2`, `preflights=2`) without trace contamination. Bounded still failed: hard errors were zero, but style-profile stayed `revise` with red `line_rhythm`, `ngram_texture`, and `punctuation`; stage snapshots show the first submission was 12 visible lines / about 1607 Chinese characters, so the source draft still began as compressed prose. A copied `finalized/` repair passed strict hard gate and style-profile (`yellow`, zero errors, zero red families), so the controller diagnosis is `source_guidance_gap`: ordinary repair can converge, but first-draft natural guidance and limited-checker readiness are still too weak. Root fixes from this case: `clean-generation-brief.md` and `SKILL.md` now state that clean-eval must not write prose first and then cut it; first drafts should directly target a middle rhythm corridor with several <=8-character breath drops and several rough long lines; `clean_run_checker.py` now uses the same <=8-character short-breath concept as style-profile and uses a stronger long-line rebalance before the second checker.

Follow-up retest `iteration-20260704-27/eval-02-food-delivery-heatstroke` used the realistic prompt with `longcat/LongCat-2.0` after commit `d45b391`. This run directly exercised the two-checkpoint development protocol. Bounded clean-eval reached the actual two-call checker boundary (`calls=2`, `preflights=2`, clean trace) but failed: first submission was still prose-shaped, stage audits stayed `fail`, bounded final had strict hard errors and style-profile `revise` with red `connectors`, `line_rhythm`, `ngram_texture`, and `punctuation`. A copied `finalized/` repair then converged after ordinary multi-round repair: strict hard gate had zero errors and style-profile was `yellow`. The controller diagnosis is `source_guidance_gap`, not readiness for blind rounds. Root fixes from this case: the earliest generation layer now treats "write prose then split it" as a failed source start, and it flags softer binary self-corrections such as `也不是疼，就是...`, `不是认识，就是...`, and `最疼的不是X，是Y` before the checker rather than leaving them for post-hoc repair.

Follow-up retest `iteration-20260704-28/eval-02-food-delivery-heatstroke` used the same realistic prompt after commit `00da12e`. Trace audit was clean, but bounded clean-eval stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`) because one binary reframe remained after repair. Stage snapshots show the first submission had two binary reframes; the generator fixed one and missed the other. The copied finalized repair cleared strict hard errors, but style-profile stayed `review` with five yellow drift families (`cognitive_mechanism`, `connectors`, `punctuation`, `structure`, `texture`) and no red families. Diagnosis is `systemic_gap`, not ready for blind rounds. Root fixes from this case: clean-run preflight now reports binary-reframe occurrence count and examples, tells the generator to scan all lines, and switches surface-only preflight repair to local replacement rather than adding scenes, short drops, body symptoms, money lines, platform facts, or other texture. Source guidance and repair references also now warn that overfilled texture/cognition can make a locally clean draft still fail finalized validation.

Follow-up retest `iteration-20260705-02/eval-02-food-delivery-heatstroke` used the same realistic prompt with a different flash model surface after commit `36fc55e`. The run exposed a different generic failure: bounded clean-eval stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`) with an underbuilt short-line grid, body length below target, early line-final comma ratio near zero, weak paragraph engine, and missing rough self-damage. The copied finalized repair cleared strict hard errors but remained style-profile `review` with red `punctuation` plus five yellow drift families, so it was not ready for blind rounds. Root fixes from this case: clean-run preflight now detects medium-length short-line grids with too few longer action/speech/thought lines, and the source guidance now treats sub-900 character drafts as incomplete while treating 900-949 character / 45-75 short-row drafts as underbuilt only when the source shape is also weak. Repair should rebuild from the source loop rather than adding isolated symptoms, app surfaces, money facts, or decorative short drops.

Follow-up retest `iteration-20260705-03/eval-04-family-dinner-argument` used a realistic family-dinner prompt and a different model surface after commit `4d0618d`. The trace was clean, but bounded clean-eval stopped at preflight before formal checker call 1/2 because the draft had 946 body Chinese characters, just below the former 950 cutoff. Controller validation still found real issues in the bounded draft (`高频词覆盖不足`, single family-theme density, and style-profile `revise` with red connector/ngram/punctuation families), so this is not a pass. Root fix from this case: standard diary still targets 950-1150 body characters, but clean-run preflight now treats `<900` as the standalone incomplete-length blocker and treats `900-949` as blocking only when paired with weak source shape such as short-row grid, too few longer action/speech/thought lines, weak connectors, weak paragraph engine, low early comma continuation, or missing rough body/social turn.

Follow-up retest `iteration-20260705-04/eval-04-family-dinner-argument` used the same realistic family-dinner prompt with another model surface after commit `8874e5b`. The trace was clean and the draft reached the two-call checker boundary (`calls=2`, `preflights=1`), which confirms the 900-949 length-boundary fix did not keep blocking near-complete drafts. The case still failed both checkpoints: bounded had no strict hard errors but style-profile stayed `revise` with red `connectors`, `line_rhythm`, and `punctuation`; copied finalized repair also remained `revise`. Manual root review found two general failure forms: a pronoun-softened binary reframe such as `我不是叔叔，我只是...`, and a 45-70-line medium-row grid with zero true <=8-character breath drops. Root fixes from this case: binary-reframe detection now covers pronoun-softened variants, clean-eval preflight blocks missing short-breath drops from 45 body lines upward, draft-gate treats missing breathing points as generated-draft risk, and `rebalance_line_rhythm.py` can split existing short landing sentences out of otherwise uniform rows without inventing content.

Follow-up retest `iteration-20260705-05/eval-05-rental-tap-key` used a realistic prompt about a broken rental faucet, buying tape, an old classmate's WeChat, and forgetting the key. The generator used `mimo/mimo-v2.5` with high reasoning. Trace audit was clean, but bounded clean-eval stopped before a formal checker call (`calls=0`, `preflights=2`): the final bounded draft had 917 body Chinese characters, 24 body lines, early comma ratio 0, weak paragraph engine, and missing rough self-damage. Copied finalized repair using rhythm scripts still failed strict hard gate and style-profile (`revise`, red `connectors`, `line_rhythm`, `ngram_texture`, `punctuation`). Diagnosis is `systemic_gap`, not blind-round-ready. Root fixes from this case: compressed-prose preflight now prioritizes running `rebalance_line_rhythm.py` before another prose rewrite, clean-generation guidance now requires actual visible breathing clusters and actual body-row counting before the first file write, rough self-damage guidance distinguishes external low-status consequence from polite "face looks bad / seems stupid" self-commentary, and high-risk generated insight captions now include `现在我意识到...` without banning natural original-corpus `意识到` usage.

Follow-up retest `iteration-20260705-06/eval-05-rental-tap-key` used the same realistic prompt with `opencode/big-pickle` and thinking enabled after commit `b48e0fa`. The bounded trace was clean, but the run stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`). The draft improved substantially in body length and line corridor and no longer showed binary reframe or prose compression, but controller recheck found two remaining source risks: a tail `算了。` acting as a learned ending button and connector spread still below the generated-draft gate. The previous rough-self-damage failure was a checker blind spot: broken card/failed door maneuver/knee sound/neighbor witnessing is a practical low-status consequence and should not force the generator to add another coarse label. Root fixes from this case: practical lockout failures such as broken cards, failed door-opening maneuvers, body sounds in a hallway, and being seen with the failed object now count as rough self-damage only when they lower body/social/practical position; plain knee pain still does not count. Clean-eval preflight now catches learned ending buttons before a formal checker call and tells the generator to replace them with an already-earned unfinished action, wrong object, route, payment, reply, or body interruption.

Follow-up retest `iteration-20260705-07/eval-12-inferred-moving-micro-hope-portable` used a realistic moving-room prompt with randomly selected `gateway/gpt-5.5`, `--variant high`, and thinking enabled after commit `7d4a87c`. The trace was clean and the run reached the actual two-call checker boundary (`calls=2`, `preflights=2`), which is better evidence than a preflight stop. Bounded still failed and is not blind-round-ready: the run-time bounded final had hard errors for prompt-shaped opening and missing breathing point, and style-profile stayed `review` with red `line_rhythm` and `punctuation`. Stage audit showed a general repair-path issue: the first checker submission already had a reasonable 60-line rhythm corridor with many true <=8-character short drops and long action/thought rows, but the normal checker still used an older 1-2-character breathing-point definition and the clean-run wrapper split/merged the draft before checker call 2, reducing the rhythm into a flatter 52-line shape. Root fixes from this case: checker breathing points now use the same true <=8-character short-drop definition as clean-eval preflight, and `clean_run_checker.py` only runs line-splitting before the final checker when the current draft is actually prose-compressed, under-lined, or long-line dominated; an already valid rhythm corridor is preserved.

Model-rotation note: `iteration-20260705-08/eval-12-inferred-moving-micro-hope-portable` attempted an invalid OpenCode model id and returned `ProviderModelNotFoundError`; record it as model unavailable, not as a generation-quality failure. Follow-up `iteration-20260705-09/eval-12-inferred-moving-micro-hope-portable` used `deepseek/deepseek-v4-pro`, `--variant high`, and thinking enabled after commit `42b6fac`. The trace was clean, but bounded clean-eval stopped at preflight before checker call 1/2 (`calls=0`, `preflights=3`). The draft was a complete article, but preflight reported too few longer action/thought rows and missing rough self-damage. Controller review found another general checker/preflight gap: visible moving-room grime such as dirty fingernails, gray-stained pants under hallway light, and a cashier noticing the narrator's hand is a practical low-status consequence, not a reason to force another coarse label. Root fixes from this case: visible grime noticed by a cashier, shopkeeper, neighbor, or passerby now counts as rough self-damage only when it lowers body/social/practical position; clean-eval preflight now lets near-miss drafts with three longer rows reach the first formal checker call instead of stopping before any actual checker evidence.

Follow-up retest `iteration-20260705-10/eval-12-inferred-moving-micro-hope-portable` used another rotated model surface after commit `5ede194`. The trace was clean but stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`). Controller review separated one true source failure from two checker/preflight false negatives: `不是那种酸，是...` is a real generated-draft binary reframe and must remain blocked; a one-to-one landlord message with `后面跟了个笑脸` is not a formulaic comment chain; ordinary moving-room mess such as wrong slippers, snot nearly coming out, dirty pants, sticky drain hair, and worn-through socks can be a low-status body/social consequence when it changes action or social exposure. Root fixes from this case: comment-chain detection now requires crowd/comment/group context for compressed reaction residues such as `跟了个`; ordinary unclean low-status consequences now count as rough self-damage and paragraph-engine signals when consequential; source guidance names these as optional examples, not required ingredients. This is a model-agnostic fix: future model differences should continue to be generalized as source shape, sentence form, fact handling, rhythm, or checker-boundary problems, never as provider-specific instructions.

Follow-up retest `iteration-20260705-11/eval-12-inferred-moving-micro-hope-portable` used `deepseek/deepseek-v4-pro`, `--variant high`, and thinking enabled after commit `1fd371e`. The trace was clean and respected `CLEAN_RUN_PREFLIGHT_STOP` (`calls=0`, `preflights=3`), so this is a valid bounded failure rather than a contaminated run. The first submission was still prose-compressed (29 body lines, 964 body characters), then rhythm scripts moved it to 47 lines; the generator added a public stomach-noise / dirty-hand shop consequence, but the preflight still missed that as rough self-damage and paragraph engine. After checker repair, the bounded draft no longer had binary reframe or comment-chain errors, but strict controller validation still reported `高频词覆盖不足`, and style-profile stayed `review` with red punctuation drift plus yellow connector/structure/texture drift. Root fixes from this case: public body noise and another person reacting to dirty hands now count as low-status consequence when socially exposed; source guidance now warns not to paste the rough consequence as one long paragraph-line; formal drafts should not stop at exactly three familiar connector families such as `其实/发现/好像`, and connector repair must come from action/reply/body/screen movement rather than synonym swapping. The current target is still not met; this remains source-guidance and rhythm/connector work before blind rounds.

Follow-up retest `iteration-20260705-12/eval-03-valentines-alone` used another rotated model surface with thinking enabled after commit `a917146`. The trace was clean and respected the preflight stop boundary (`calls=0`, `preflights=3`), so the bounded checkpoint is a valid source-guidance failure. The bounded final had hard errors for `高频词覆盖不足`, `粗粝自毁信号不足`, and `段落发动机信号偏弱`; style-profile stayed `revise` with red `connectors`, `line_rhythm`, and `punctuation`. A copied finalized repair then passed strict hard gate and style-profile validation (`yellow`, zero red families), so the controller diagnosis is `source_guidance_gap`, not a repair-system failure. Manual root review found two model-agnostic causes in the bounded path: social-feed prompts still encouraged a feed montage (`多条朋友圈/点赞取消/最新视频`) instead of one screen surface that changes action, and wrapper preflight text still tempted the generator to repeat diagnostic metrics as a checklist. Root fixes from this case: Layer 0 now treats 朋友圈/short-video/social comparison as one pressure surface before the first checker, not a scene slate, and the clean-run wrapper now explicitly tells generation agents not to summarize, quote, or enumerate diagnostics as a TODO list.

Follow-up retest `iteration-20260705-13/eval-06-double-eleven-parcel` used `deepseek/deepseek-v4-flash`, `--variant high`, and thinking enabled after commit `49c2138`. The bounded run respected the stop boundary but stopped before formal checker call 1/2 (`calls=0`, `preflights=3`), so it is still a source-guidance failure and not blind-round-ready. The bounded final remained underbuilt at 790 body Chinese characters, had zero long action/speech/thought rows, high period density, sparse connector spread, no rough self-damage, and no paragraph engine. A copied finalized repair then passed strict hard gate and style-profile validation (`yellow`, zero red families), so the controller diagnosis is again `source_guidance_gap`: ordinary repair can converge, but clean-eval first-draft guidance remains too weak. Controller recheck also found one checker false positive: `工位` inside a classmate's social-feed post was being treated as the narrator's current office identity. Root fixes from this case: current-office detection now distinguishes first-person current office persona from third-person/social-feed office surfaces; Layer 0 now states that `complete article` is not complete prompt inventory, so multi-item prompts should not be rendered as one neat scene per prompt noun; object/shopping/parcel failures must change body, money, route, reply, room, or social position rather than merely reporting that an item was wrong. Trace validation now records visible planning/diagnostic chatter as a warning because some model/provider runs print process text even when the final article is clean.

Follow-up retest `iteration-20260705-15/eval-09-classmate-wedding-invitation` used a rotated model surface after commit `ab5e3b7`. The bounded run checked the clean-eval marker first and stopped correctly at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`). It is not blind-round-ready. The draft was complete in length and line corridor but stayed too close to one-screen chronology: WeChat invitation, ticket prices, gift ledger, refusal, venue photo, old chat, then a literary phone/bed ending. A copied finalized repair cleared strict hard errors, but style-profile remained `review` with red `punctuation` and yellow `cognitive_mechanism`, `connectors`, `structure`, and `texture`; controller diagnosis is `systemic_gap`, not source-guidance-only. Controller recheck also found one checker false positive: one-to-one `又发了个红包` was treated as comment-chain residue. Root fixes from this case: comment-chain detection now requires crowd/comment/group context for compressed reaction residues such as `又发了个`, while true group/comment chains remain blocked; Layer 0 and runtime guidance now treat invitation/wedding/reunion/social-comparison prompts as message-pressure surfaces, not chat transcripts or price ledgers. These fixes stay model-agnostic: model rotation is used only to expose source-shape, sentence-form, fact-handling, rhythm, tool-routing, or checker-boundary problems.

Follow-up retest `iteration-20260705-16/eval-10-absurd-coincidence-projection` used `mimo/mimo-v2.5-pro`, `--variant high`, and thinking enabled after commit `555b274`. The bounded generator loaded `runtime-brief.md` before the first complete `draft.md`, so the trace was contaminated even though it later used the clean-eval marker and stopped correctly at preflight (`calls=0`, `preflights=3`). The bounded draft still had the soft AI binary reframe `其实不是想X，就是Y`, theatrical quote-dialogue, no rough self-damage under strict gate, and style-profile `revise`. A copied finalized repair cleared strict hard errors, and checker recheck confirmed that a vendor's quoted `我儿子` should not be treated as the narrator's child identity, but style-profile still failed with red `line_rhythm`, `ngram_texture`, and `punctuation`. Diagnosis remains `systemic_gap`, not blind-round-ready. Root fixes from this case: JSONL trace audit now ignores dumped skill/reference bodies while still catching actual pre-draft reference reads; clean-eval source guidance now has a short reference-stop rule after `clean-generation-brief.md`; stranger/shopkeeper/vendor prompts now forbid quoted transcript shape and require action/body/payment residue; family-identity checking distinguishes narrator-owned spouse/child identity from third-person speech; and `rebalance_line_rhythm.py` now preserves existing paragraph blocks instead of collapsing repaired drafts into a continuous prose block.

Follow-up retest `iteration-20260705-17/eval-10-absurd-coincidence-projection-post-source-gate` used the same rotated model surface after commit `9b82bc0`. The reference-stop fix worked: the bounded trace did not show early `runtime-brief.md` contamination. The run still stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`), so it is not ready for blind rounds and does not test the two actual checker calls. Controller summary classified the bounded result as `invalid` with first-submission `fail`; strict hard gate on bounded final still showed ordinary dialogue quotes, insufficient connector spread, missing rough self-damage, and zero early line-final comma rhythm. Style-profile stayed `review` with red `line_rhythm` and `punctuation`. Root fixes from this case: Layer 0 now names the "complete small realist story" failure mode, clean-eval preflight flags theatrical quoted dialogue and caption-like polished similes, and runtime repair guidance cuts stranger/vendor prompt performance into embedded speech plus payment/object/body residue. This is model-agnostic and must not be turned into provider-specific instructions.

Follow-up attempt `iteration-20260705-18` randomly selected an invalid OpenCode model id, but OpenCode returned an unexpected server error before generation, so it is recorded as model unavailable/invalid, not as a prose-quality result. Follow-up retest `iteration-20260705-19/eval-10-absurd-coincidence-projection-after-dialogue-simile-gate` used `mimo/mimo-v2.5`, `--variant high`, and thinking enabled after commit `fabe3e9`. The previous source fixes helped: the bounded draft no longer had theatrical quote dialogue or polished nail/well simile captions. It still stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`) because the draft was a near-miss short article: 892 body Chinese characters, 37 body lines, and only `其实/觉得` connector families. Strict hard gate had one real remaining error (`高频词覆盖不足`) and style-profile stayed `review` with red `line_rhythm`. Root fix from this case: Layer 0 now treats 35-44-line / 850-949-character near-miss drafts with thin connectors as not ready to save; the generator should add one full off-axis life cluster before first `draft.md`, not wait for preflight or patch with one sentence.

Follow-up retest `iteration-20260705-20/eval-10-absurd-coincidence-projection-after-nearmiss-gate` used `mimo/mimo-v2.5-pro`, `--variant high`, and thinking enabled after commit `150a9ac`. The run still stopped at preflight before formal checker call 1/2 (`calls=0`, `preflights=3`), so it remains not ready for blind rounds. The bounded final had 943 body Chinese characters, 37 body lines, only three long action/speech/thought rows, weak rough self-damage and paragraph engine, and style-profile `revise` with red `connectors`, `line_rhythm`, and `punctuation`. The reported `binary_reframe` example was a different surface: `是不是...就是...` in a vendor identity-probe line, not the core `不是X，是Y` AI reframe. Root fix from this case: the checker now separates true `不是X，是Y` binary reframes from prompt-performing identity dialogue, and Layer 0 now tells generators not to let strangers/vendors recite school, place, career, salary, or success-comparison facts through neat `你是不是...就是...` probes. This is a model-agnostic source-shape fix; model rotation should continue to expose general failure patterns, not create model-specific runtime branches.

Follow-up development pass after `iteration-20260706-63/eval-03-2023-valentines-alone` found the same bounded failure family in a different prompt: the generator respected trace protocol but still produced an 80+ row short-line grid with zero natural long action/speech/thought rows, a prompt-prop title/opening around the wrong food, thin connector spread, and too little rough self-damage. This is still source-guidance failure, not blind-round readiness. Root fixes from this pass: `clean-generation-brief.md` and the Clean-Eval Minimum now require the first saved standard-diary candidate to contain concrete breathing clusters with several rough long rows before `draft.md` is written; private-holiday/romance-feed/wrong-food/takeout prompts now require a side engine before the order/feed prop can control title, opening, or ending; `clean_run_checker.py` now gives an explicit `NEXT_ACTION` rhythm-reset instruction for short-line grids and zero/too-few long rows. The checker was also tightened against two false positives discovered by this run: ordinary ambient `有人说话` no longer counts as formulaic comment-chain residue without group/comment/feed context, and a local shop owner or vendor `她搬走了` no longer counts as an invented mother/family loss merely because the article has distant family/holiday context. True group/comment chains and unsupported mother death/loss claims remain blocked.

This README should be updated whenever the runtime architecture, validation protocol, or latest evidence boundary changes. A fresh pass/fail claim still requires new verification and fresh clean-eval generations after the latest commit.
