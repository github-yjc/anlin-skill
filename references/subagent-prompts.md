# Legacy Compatibility Pointer

This file intentionally remains as a compatibility pointer for old controller notes, development logs, and links that used the former filename `subagent-prompts.md`.

The active controller judge templates now live in:

- `references/judge-prompt-templates.md`

Do not treat this legacy filename as a runtime dependency on any special parallel-agent capability. The active templates can be used by a human reviewer, a separate model context, or any controller that can isolate inputs.
